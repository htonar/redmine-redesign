# CLAUDE.md

Контекст для работы над проектом - что делаем и почему, чтобы не переоткрывать
решения заново в каждой сессии.

## Что это

Собственный React-фронтенд для Redmine поверх его REST API. Не форк Redmine, не
плагин - отдельное SPA, говорящее с любым Redmine-инстансом через `/xxx.json`
эндпоинты. Дизайн - по мотивам [Behance-концепта](docs/design.md), не штатный UI
Redmine.

## Стек и почему так

- **Vite + React + TypeScript**, npm - стандартный выбор, без экзотики.
- **API-клиент - свой, тонкий, поверх сгенерированных типов**, а не готовая
  npm-библиотека: `redmine-ts` и `axios-redmine` заброшены/deprecated (см.
  [docs/redmine-api.md](docs/redmine-api.md)). Вместо них - OpenAPI-спека
  [d-yoshi/redmine-openapi](https://github.com/d-yoshi/redmine-openapi) →
  `openapi-typescript` (генерация типов) → `openapi-fetch` (рантайм-клиент).
  Спеку обновлять релизами оттуда, не редактировать `schema.d.ts` руками.
- **UI-кит - shadcn/ui на Radix Primitives** + своя тема поверх (см. ниже про
  дизайн). Выбрано вместо полностью самописных примитивов - быстрее получить
  рабочий и доступный (a11y) результат, кастомизация цветов/шрифтов все равно
  нужна под референс.

## Заметка: TypeScript 6.0 и peer deps

Vite-шаблон подтянул TypeScript 6.0 (bleeding edge), а часть экосистемы
(`openapi-typescript`, CLI `shadcn`) еще требует `^5.x` в peerDependencies.
Это конфликт версий пакетов, не реальная несовместимость - поэтому в `.npmrc`
стоит `legacy-peer-deps=true`. Если при обновлении зависимостей конфликт уйдет
сам (экосистема догонит TS 6), `.npmrc` можно убрать.

## Важное ограничение: авторизация и 2FA

Redmine REST API **не поддерживает интерактивный 2FA-челлендж**. Если у
пользователя включена двухфакторная аутентификация, HTTP Basic по логину/паролю
жестко отклоняется сервером (`401`) - это защита от обхода 2FA через API, а не
баг клиента. Работает только **API-ключ**.

Решение: клиентское приложение авторизуется **только по API-ключу**
(`X-Redmine-API-Key`). Логин/пароль в `src/api/client.ts` поддержан на уровне
типов (Basic auth) на случай не-2FA сценариев в будущем, но экран логина в UI
предлагает только ввод API-ключа - не строить форму логин/пароль как основной
путь, она не будет работать для аккаунтов с 2FA (включая аккаунт автора проекта).

## CORS и прокси-бэкенд

Проверено вручную на реальном инстансе (redmine.codestetic.com): Redmine не
отдает `Access-Control-Allow-Origin` ни на обычный запрос, ни на preflight
`OPTIONS`. Браузер блокирует прямой fetch из SPA с ошибкой `Failed to fetch` -
это не редкий случай, а, похоже, поведение по умолчанию (в коре Redmine нет
встроенной поддержки CORS). Полагаться на то, что у пользователя CORS настроен
на сервере, нельзя.

Решение - свой прокси-бэкенд, `server/` (Node.js + Hono, отдельный пакет со
своим `package.json`, не часть фронтового билда):

- Клиент шлет запросы на `${VITE_REDMINE_PROXY_URL}/proxy/...` вместо прямого
  обращения к Redmine, с заголовком `X-Redmine-Target: <baseUrl>` - тот же
  адрес, что пользователь ввел на экране логина. Реализовано в
  `src/api/client.ts` (`proxyUrl` опция) и `src/contexts/AuthContext.tsx`
  (читает `VITE_REDMINE_PROXY_URL` из env, см. `.env.example`).
- Прокси форвардит запрос 1:1 в целевой Redmine и добавляет CORS-заголовки в
  ответе (`hono/cors`, origin из `ALLOWED_ORIGIN`).
- Таргет **динамический** (любой Redmine, не захардкожен), но целевой хост
  обязан быть в `ALLOWED_REDMINE_HOSTS` (см. `server/.env.example`) - иначе
  прокси превращается в открытый SSRF-прокси, который можно использовать для
  доступа к произвольным внутренним хостам. Без непустого `ALLOWED_REDMINE_HOSTS`
  прокси отклоняет вообще все запросы (500), это осознанный fail-closed.
- Если `VITE_REDMINE_PROXY_URL` не задан - клиент работает как раньше, напрямую
  (годится для локальной разработки или инстансов с настроенным CORS).
- Прокси не хранит и не логирует API-ключи - только форвардит заголовок
  `X-Redmine-API-Key` дальше, транзитом.

Запуск для разработки - два процесса: `npm run dev` в корне (фронт) и `npm run
dev` в `server/` (прокси). Оба нужны, чтобы реально сходить в чей-то Redmine.

## Структура

```
api/redmine-openapi.yaml   # OpenAPI-спека Redmine REST API (источник правды)
src/api/schema.d.ts        # сгенерированные TS-типы - не редактировать руками
src/api/client.ts          # фабрика типизированного клиента (createRedmineClient)
server/                    # прокси-бэкенд для обхода CORS (Node.js + Hono) - только веб-сборка
src-tauri/                 # десктоп-обертка (Tauri/Rust) - см. "Бэклог: паковка..." ниже
docs/redmine-api.md        # как устроена спека/клиент, как обновлять
docs/design.md             # дизайн-язык, палитра, компоненты из референса
```

## Статус / следующие шаги

- [x] Инициализация проекта, git
- [x] OpenAPI-спека + генерация типов + тонкий клиент
- [x] Дизайн-направление зафиксировано (`docs/design.md`)
- [x] Подключен shadcn/ui (стиль radix-nova) на Tailwind v4, тема настроена под
  палитру из `docs/design.md` (`src/index.css`), алиас `@/*` -> `src/*`
- [x] Базовые layout-компоненты: `Sidebar`, `Topbar`, `AppShell`
  (`src/components/layout/`), `StatCard` (`src/components/StatCard.tsx`)
- [x] Экран логина (только API-ключ, валидация через `GET /my/account.json`).
  `AuthProvider`/`useAuth` (`src/contexts/AuthContext.tsx`) - хранит сессию в
  localStorage (`src/lib/auth-storage.ts`), при старте тихо проверяет
  сохраненный ключ и разлогинивает, если он больше не работает. Сетевые ошибки
  (недоступный хост/CORS) и 401 показываются пользователю разными сообщениями -
  см. `fetchAccount` в `AuthContext.tsx`.
- [x] Прокси-бэкенд для обхода CORS (`server/`) - см. раздел "CORS и
  прокси-бэкенд" выше. Проверено сквозным прогоном на реальном инстансе.
- [x] Роутинг (`react-router`, `BrowserRouter`). `src/pages/AppLayout.tsx` -
  общий каркас (сайдбар/топбар) с `<Outlet>`, реальные маршруты -
  `/issues`, `/issues/:id`, `/time` и `/dashboard` (см.
  `src/components/layout/nav-items.ts` - остальные пункты сайдбара без `path`
  показаны неактивными).
- [x] **Список задач с фильтрами/сортировкой/сохраненными видами**
  (`src/pages/IssuesPage.tsx`). Фильтры: исполнитель (мои/все), статус
  (открытые/закрытые/все), проект - через общий селектор в Topbar
  (`src/hooks/useProjects.ts`, реальные проекты из `GET /projects.json`).
  Сортировка по клику на заголовок колонки. Подгрузка "Показать еще"
  (`src/hooks/useIssues.ts`). Сохраненные виды - свои, в localStorage
  (`src/hooks/useIssueViews.ts`, `src/lib/issue-views-storage.ts`), диалог
  сохранения - `src/components/issues/SaveViewDialog.tsx`. Дефолт при заходе -
  "мои открытые задачи". Проверено сквозным прогоном на реальном инстансе:
  фильтры, сортировка, сохранение/применение вида - все отработало корректно.
  **Баг найден и починен позже (2026-08-13)**: таблица не показывала трекер
  (Bug/Feature/...) вообще ни одной колонкой - пользователь не мог отличить
  баг от обычной задачи в списке. Добавлена сортируемая колонка "Трекер"
  между "Тема" и "Приоритет" (`issue.tracker?.name` - поле уже приходило в
  `IssueSummary`, просто не рендерилось).
- [x] **Учет трудозатрат** (`src/pages/TimeTrackingPage.tsx`, маршрут `/time`).
  Просмотр - `GET /time_entries.json` через `useTimeEntries`
  (`src/hooks/useTimeEntries.ts`, пагинация "показать еще" по образцу
  `useIssues.ts`), фильтры: свои/все (`user_id=me`), проект (общий селектор в
  Topbar), период - шорткаты Redmine для `spent_on` (`t`/`w`/`m`/без фильтра).
  Записи сгруппированы по дням на фронте (сумма часов за день и по всем
  загруженным записям) - отдельного summary-эндпоинта в API нет, см. "Ключевые
  фичи". Быстрый ввод и правка - `LogTimeDialog`
  (`src/components/time/LogTimeDialog.tsx`): задача (по номеру) или проект,
  дата, часы, вид деятельности (`GET /enumerations/time_entry_activities.json`
  через `useTimeEntryActivities`), комментарий; один и тот же диалог для
  создания (`POST`) и правки (`PUT`) записи. Удаление - `DELETE` с
  подтверждением через `window.confirm` (отдельного диалога подтверждения в
  проекте пока нет). Привязано к карточке задачи (см. следующий пункт) -
  кнопка "Залогировать время" там открывает тот же `LogTimeDialog` с уже
  подставленными `defaultIssueId`/`defaultProjectId` (проп `defaultIssueId`
  добавлен специально под этот сценарий - до этого диалог умел
  предзаполнять задачу только в режиме правки существующей записи, не для
  новой). Проверено сквозным прогоном (GET-запросы) на реальном инстансе
  redmine.codestetic.com - активности и time_entries (с фильтрами и без)
  возвращают ожидаемую форму данных.
- [x] **Карточка задачи** (`src/pages/IssueDetailPage.tsx`, маршрут
  `/issues/:id`; строки в `IssuesPage` теперь кликабельны и ведут туда).
  Данные - `GET /issues/{id}.json?include=journals,allowed_statuses`
  (`getIssue` в `src/api/issues.ts`, хук `useIssue`). Показывает: тему,
  трекер, приоритет, проект/автора/исполнителя/категорию/версию, даты,
  готовность (`done_ratio` через `Progress`), оценку и потраченные часы
  (`estimated_hours`/`spent_hours` - уже приходят в самом issue, отдельный
  запрос не нужен), описание, историю изменений и комментарии (`journals`,
  в хронологическом порядке; изменения полей рендерятся как "Поле: было →
  стало" с русским лейблом для частых полей, иначе - сырое имя атрибута).
  Смена статуса - `Select` из `allowed_statuses` (это переходы, реально
  доступные текущему пользователю по workflow, а не полный список статусов),
  добавление комментария - `Textarea` + `PUT .../{id}.json` с `notes`
  (`updateIssue`) - оба остаются мгновенными контролами в шапке, вне режима
  правки ниже. Отдельного списка time_entries по конкретной задаче на
  карточке нет (только сумма `spent_hours` и кнопка логирования).
- [x] **Полная правка задачи** - кнопка "Редактировать" в шапке
  `IssueDetailPage` переключает блок метаданных+описания на общую
  форму `IssueFormFields` (`src/components/issues/IssueFormFields.tsx`):
  тема, трекер, приоритет, исполнитель, категория, версия, даты, готовность,
  оценка часов, описание. Статуса в форме сознательно нет - он и так меняется
  мгновенно через `allowed_statuses` в шапке, дублировать негде. Проект
  задачи не меняется (нет селектора). "Сохранить" собирает патч только из
  изменённых полей (`diffFormValues` в `IssueDetailPage.tsx`) и шлёт через
  расширенный `updateIssue` (`src/api/issues.ts`, полное тело
  `PUT /issues/{id}.json`); "Отмена" просто сбрасывает локальный state, без
  запроса.
- [x] **Создание задачи** - кнопка "Добавить задачу" на `IssuesPage`
  открывает `CreateIssueDialog` (`src/components/issues/CreateIssueDialog.tsx`,
  по образцу `LogTimeDialog`): обязательны проект (дефолт - текущий фильтр
  в Topbar) и тема, остальное - тот же `IssueFormFields`. `POST /issues.json`
  (`createIssue`), после успеха - переход на карточку новой задачи. Общие
  справочные хуки для обеих форм (создание/правка):
  `src/hooks/useTrackers.ts`, `useIssuePriorities.ts`, `useProjectMembers.ts`
  (участники проекта - источник для "Исполнитель"; упрощение - без фильтра
  по роли "может быть назначен", REST API это не отдаёт), `useProjectCategories.ts`,
  `useProjectVersions.ts`.
- [x] **Дашборд** (`src/pages/DashboardPage.tsx`) - реальные данные вместо
  статичного демо: 4 счетчика через `listIssues` (мои открытые, все открытые,
  мои всего, закрыто - с учетом текущего проекта из Topbar) и список из 5
  недавно обновленных открытых задач на текущем пользователе, строки
  кликабельны и ведут в карточку задачи. Без трендов/спарклайнов - для них
  нет источника исторических данных, придумывать не стали (в отличие от
  прежнего статичного контента, где проценты были фейковыми).
- [x] **Тёмная тема** - `ThemeContext`/`useTheme` (`src/contexts/ThemeContext.tsx`,
  по образцу `AuthContext`): `theme: 'light' | 'dark'`, дефолт из
  `localStorage` либо `prefers-color-scheme`, переключение - класс `.dark` на
  `document.documentElement` (токены обеих тем уже были в `src/index.css`, не
  хватало только переключателя). Тоггл - иконка в `Topbar`
  (`src/components/layout/Topbar.tsx`), состояние прокидывается из
  `AppLayout.tsx` через `AppShell`. Выбор сохраняется между перезагрузками.
- [x] **Docker** - `Dockerfile` (корень, фронт: `vite build` + nginx,
  `VITE_REDMINE_PROXY_URL` через build arg), `server/Dockerfile` (прокси,
  запуск через `tsx` как и локально), `docker-compose.yml` - полный локальный
  стенд для тестирования (свой Redmine + Postgres + прокси + фронт). `proxy`
  подключен через `network_mode: service:redmine` (общий network namespace с
  Redmine) - иначе `http://localhost:3000`, тот же адрес, что вводится на
  экране логина, у прокси внутри контейнера резолвился бы не туда; прокси
  намеренно разрешает http только для localhost/127.0.0.1 (см. "CORS и
  прокси-бэкенд"), и `network_mode: host` в этих целях ненадёжен (проверено -
  на окружении с виртуализированным docker-демоном host-сеть не пробрасывается
  на реальный хост). Подробности и шаги первого запуска - README, раздел
  "Docker".
- [x] **Локальный Redmine: два docker-compose файла, разные задачи** -
  `docker-compose.yml` выше поднимает **весь стек** (Redmine + Postgres +
  прокси + собранный фронт в nginx) - для сквозной проверки того, что реально
  увидит пользователь. `docker-compose.redmine.yml` (см. раздел "Локальный
  Redmine для тестирования" ниже) - **только сам Redmine** на SQLite, без
  прокси/фронта - для разработки с `npm run dev` напрямую, когда не нужно
  пересобирать образ фронта на каждое изменение. Оба варианта легитимны для
  разных сценариев, держать оба.
- [x] **Создание и правка задачи проверены сквозным прогоном** - на локальном
  Redmine, поднятом через `docker-compose.yml` (важный нюанс: у свежего
  Redmine пустая база не содержит дефолтных трекеров/статусов/приоритетов -
  нужно один раз выполнить `rake redmine:load_default_data` в контейнере,
  иначе создание задачи падает с "Tracker cannot be blank"/"Status cannot be
  blank"; и трекеры должны быть привязаны к проекту в его настройках - при
  создании проекта до загрузки дефолтных данных список трекеров пуст).
  Создание задачи (тема, трекер, приоритет, описание) и правка темы через
  форму `IssueFormFields` - оба сработали, `POST`/`PUT` дошли до Redmine,
  карточка задачи отражает изменения.
- [x] **Создание и полная правка задачи проверены сквозным прогоном** - на
  локальном одноразовом Redmine (`docker-compose.redmine.yml`, см. раздел
  "Локальный Redmine для тестирования" ниже). Оба флоу отработали через UI
  (не curl): `POST /issues.json` создал задачу и приложение перешло на её
  карточку, `PUT /issues/{id}.json` из формы правки сохранил изменённые тему/
  описание/готовность. По пути найден и починен реальный баг (не окружения) -
  см. следующий пункт.
- [x] **Баг: текущий пользователь выпадал из списка "Исполнитель"** -
  `GET /projects/{id}/memberships.json` не включает пользователя, если он не
  оформлен формальным участником проекта (для admin-аккаунтов Redmine это
  норма: создатель проекта не добавляется в участники автоматически). При
  этом `POST /issues.json` с `assigned_to_id` = id этого пользователя
  прекрасно проходит (проверено напрямую через API, минуя UI) - т.е. список
  участников врал о том, кого реально можно назначить. Фикс -
  `useProjectMembers` (`src/hooks/useProjectMembers.ts`) теперь принимает
  третьим параметром текущего пользователя (`AuthUser` из `useAuth()`) и
  подмешивает его в начало списка, если API его не вернул, с пометкой "(я)".
  Прокинуто в оба места использования - `CreateIssueDialog` и
  `IssueDetailPage`. Проверено в браузере: "Redmine Admin (я)" появляется в
  дропдауне.
- [x] **Подзадачи и связи задач** (`src/pages/IssueDetailPage.tsx`, новая
  карточка "Подзадачи и связи" - рендерится всегда, независимо от режима
  правки, между блоком метаданных/описания и карточкой "Время"). Родитель/
  подзадачи - через `parent_issue_id` (`getIssue` теперь запрашивает
  `include=children`, `IssueFieldsInput.parentId` в `src/api/issues.ts`):
  показ текущего родителя со снятием (×), добавление подзадачи - по номеру
  задачи (шлёт `parent_issue_id` на саму эту задачу, отдельного эндпоинта не
  нужно). Связи (`relates`/`blocks`/`blocked`/`precedes`/`follows`/
  `duplicates`/`duplicated`/`copied_to`/`copied_from`) - `include=relations` +
  новые `createIssueRelation`/`deleteIssueRelation`
  (`POST/DELETE /issues/{id}/relations`, `DELETE /relations/{id}`), лейблы и
  разворот типа для обратного направления - `src/lib/issue-relations.ts`
  (Redmine хранит связь направленно `issue_id → issue_to_id`, показывать её
  нужно корректно с обеих сторон). У родителя/связей отдаётся только `{ id }`
  без темы - подгружается отдельно, по одному `GET` на id, хук
  `src/hooks/useIssueSummaries.ts` (батч-фильтра по списку id в API нет, см.
  комментарий в файле). Проверено сквозным прогоном в браузере на локальном
  Redmine: подзадача добавляется и видна с обеих сторон, родитель
  назначается/снимается, связь добавляется и корректно отображается в обратную
  сторону (`blocks` ↔ `blocked`), связь удаляется по ×. Замечено (не баг
  приложения, а поведение самого Redmine) - при назначении родителя сервер
  сам пересчитывает `priority`/`done_ratio` родителя по подзадачам, и
  `relates`-связь между задачами молча снимается, если они становятся
  родителем/потомком (связь такого типа между ними невалидна).
- [x] **Баг: `include` как несколько задач ломал их обработку** -
  `openapi-fetch` по умолчанию сериализует массивы в query как повторяющиеся
  ключи (`include=journals&include=children`), а Redmine (Rails/Rack) ждёт
  один параметр через запятую (`include=journals,children`) - с повторяющимися
  ключами видит только последнее значение, остальные молча теряются. Всплыло
  на `include=journals,allowed_statuses,children,relations` в `getIssue`:
  подзадачи (`children`) не показывались в UI, хотя связь существовала в БД
  (подтверждено прямым запросом к API в обход клиента). Фикс - глобальный
  `querySerializer: { array: { style: "form", explode: false } }` в
  `createClient()` (`src/api/client.ts`), затрагивает всех вызывающих
  `getIssue`, не только новую фичу. Это, вероятно, и есть причина более
  раннего нерасследованного наблюдения (пустая история изменений после правки
  задачи в одной из прошлых сессий) - `journals` был одним из молча
  терявшихся include.
- [x] **Вложения к задаче** (новая карточка "Вложения" в
  `IssueDetailPage.tsx`, между "Подзадачи и связи" и "Время"; API-слой -
  `src/api/attachments.ts`). Redmine отдает вложения двухшаговым процессом:
  сначала сырые байты файла через `POST /uploads.json?filename=...&content_type=...`
  (`uploadAttachment`) - возвращает `token`, который затем передается в
  `uploads` при создании/правке задачи (`IssueFieldsInput.uploads`,
  `createIssue`/`updateIssue` в `src/api/issues.ts`); только загрузка байтов
  без второго шага задачу не меняет. `getIssue` запрашивает
  `include=...,attachments`, чтобы список уже прикрепленных файлов приходил
  вместе с задачей. Скачивание - `GET
  /attachments/download/{attachment_id}/{filename}` через типизированный
  клиент с `parseAs: "blob"` и локальным `object URL`, а не прямая ссылка на
  `content_url` - та ведет напрямую на Redmine-хост в обход прокси и не
  понесет заголовок `X-Redmine-API-Key`. Удаление - отдельный `DELETE
  /attachments/{id}.json` (`deleteAttachment`), не через поле задачи.
  Форматирование размера - `formatFileSize` в `src/lib/utils.ts`.
  **Баг найден и починен по пути**: `POST /uploads.json` отвечал `406 Not
  Acceptable`, если `Content-Type` запроса выставлялся в MIME-тип самого
  файла (например `text/plain` для `.txt`) - Redmine ждет строго
  `application/octet-stream` у тела запроса вне зависимости от реального типа
  файла, который передается отдельно через query-параметр `content_type`.
  Проверено изолированным `fetch` мимо клиента: тот же запрос с
  `Content-Type: text/plain` → 406, с `application/octet-stream` → 201.
  Заодно понадобился отдельный фикс в `client.ts` - глобальная мидлварь
  раньше безусловно перетирала `Content-Type` на `application/json` на каждом
  запросе, теперь не трогает его, если он уже явно задан вызывающим кодом.
  Проверено сквозным прогоном в браузере на локальном Redmine: загрузка файла
  через "Прикрепить файл" (реальный upload через `file_upload` в hidden
  `<input type="file">`), файл появляется в списке с именем и размером,
  скачивание отдает 200 и корректно инициирует сохранение файла браузером,
  удаление по × убирает файл из списка и это отражается в истории изменений
  задачи (`journals`).
- [x] **Наблюдатели (watchers)** (новая карточка "Наблюдатели" в
  `IssueDetailPage.tsx`, между "Вложения" и "Время"; API-слой -
  `src/api/watchers.ts`). Список наблюдателей приходит вместе с задачей -
  `getIssue` расширен до `include=...,watchers` (это уже `{id, name}[]`,
  отдельного summary-хука не нужно, в отличие от родителя/связей). Подписка -
  `POST /issues/{id}/watchers.json` с телом `{ user_id }` (не обёрнуто в
  ключ `watcher`), отписка - `DELETE
  /issues/{id}/watchers/{user_id}.json`; оба отвечают `204`. В карточке -
  переключатель "Наблюдать"/"Не наблюдать" за себя (в шапке карточки),
  список текущих наблюдателей с удалением по ×, добавление - через `Select`
  из участников проекта (тот же источник, что и "Исполнитель", с "(я)" для
  текущего пользователя, см. баг про `useProjectMembers` выше).
  **Баг найден и починен по пути (в прокси, не в фичах)**: `DELETE
  /watchers/{user_id}.json` и `POST /watchers.json` легитимно отвечают
  `204 No Content`, но прокси (`server/src/index.ts`) ронял именно такие
  ответы - `Response`-конструктор в Node (undici) кидает исключение при
  `body` не `null` вместе со статусом `204/205/304`, а
  `upstreamResponse.body` всегда объект `ReadableStream`, даже для пустого
  тела. Первый фикс (передавать `null` вместо тела для этих статусов) решил
  проблему лишь частично - без явного `upstreamResponse.body?.cancel()`
  недочитанный поток портил keep-alive соединение в пуле undici, и уже
  СЛЕДУЮЩИЙ запрос через него падал непредсказуемо (`503`) - отсюда
  нестабильность, которую было трудно поймать: один и тот же клик иногда
  проходил, иногда нет, а `curl`/одиночные `fetch` не воспроизводили баг
  вовсе (каждый вызов - новое соединение, без реюза). Полный фикс - явная
  отмена потока (`await upstreamResponse.body?.cancel()`) перед тем, как
  вернуть `null`-тело; заодно обёрнут сам `fetch()` до Redmine в try/catch
  (`502` вместо необработанного исключения на реальном сетевом сбое).
  Диагностика заняла много времени именно из-за нестабильности - помогло
  временное логирование в `authMiddleware.onRequest`/`onResponse`
  (`src/api/client.ts`) и прогон одного и того же запроса через реальный
  клик в UI десятки раз подряд, а не единичная проверка. Проверено сквозным
  прогоном в браузере на локальном Redmine: подписка/отписка за себя,
  добавление наблюдателя из списка участников, удаление по × - все пути,
  каждый по несколько раз подряд, без единого `503`; состояние верно
  переживает перезагрузку страницы.
- [x] **Лента активности на дашборде** (новая карточка "Активность" в
  `DashboardPage.tsx`, под "Недавно обновленные мои задачи" - тот же путь
  `/dashboard`, за которым в сайдбаре уже стоит пункт "Активность", см.
  `nav-items.ts`). Отдельного activity-эндпоинта в REST API Redmine нет
  (штатная страница `/activity` в самом Redmine не имеет REST-аналога),
  поэтому лента собирается на фронте (`useActivityFeed`,
  `src/hooks/useActivityFeed.ts`): берутся N=10 последних обновленных задач
  на пользователе (`GET
  /issues.json?assigned_to_id=me&sort=updated_on:desc&status_id=*`, как и
  предполагалось в этом же пункте плана раньше), для каждой отдельным легким
  запросом подгружаются `journals` (`getIssueJournal` в `src/api/issues.ts` -
  `include=journals` без лишних `allowed_statuses/children/relations/...`,
  которые для ленты не нужны и только увеличили бы вес N+1 запросов). Записи
  всех задач сливаются в одну ленту, сортируются по `created_on` desc,
  показываются первые 20; журналы без текста и без изменений полей
  отфильтровываются, задача без единого journal-события в ленте не
  появляется (это лента *изменений*, не дублирует таблицу "Недавно
  обновленные" выше - та показывает вообще все последние задачи, включая
  свежесозданные без единой правки). Одна упавшая по сети задача не роняет
  всю ленту (`.catch(() => null)` на уровне отдельного запроса). Клик по
  заголовку записи ведёт на карточку задачи.
  **Рефакторинг по пути**: рендер записи истории (`FIELD_LABELS`,
  `formatDateTime`, разметка одной записи) раньше жил только в
  `IssueDetailPage.tsx` - вынесен в переиспользуемые `src/lib/journal-format.ts`
  и `src/components/issues/JournalEntry.tsx` (принимает опциональный `header`
  для ссылки на задачу - нужен только в ленте на дашборде, где записи из
  разных задач вперемешку). `IssueDetailPage.tsx` переведён на них же -
  поведения не поменялось, дублирования не стало. По пути добавлены понятные
  подписи для `parent_id`/`child_id` в `FIELD_LABELS` (записи про
  назначение/снятие подзадачи, которых не было в исходном наборе лейблов).
  **Процессная ошибка, пойманная и исправленная в этом же заходе**: при
  переносе `formatDateTime` в общий модуль забыли поправить один из двух
  сайтов использования в `IssueDetailPage.tsx` (карточка "Обновлено" в шапке
  задачи) - `npx tsc --noEmit` (без `-b`) молча пропустил это, потому что
  корневой `tsconfig.json` в этом проекте - solution-файл (`"files": []` +
  `references`), а не сам конфиг компиляции; без `-b` эта команда фактически
  ничего не проверяет. Баг всплыл только в браузере (`ReferenceError:
  formatDateTime is not defined`, пустая карточка задачи). Фикс - добавлен
  пропущенный импорт; **вывод на будущее: типа-проверка в этом репо - `npx
  tsc -b --noEmit` (или `npm run build`), не голый `npx tsc --noEmit`** - тот
  ничего не гарантирует. Проверено сквозным прогоном в браузере после фикса:
  обе страницы (дашборд с лентой, карточка задачи с историей) рендерятся
  корректно, консоль и network - чисто, `npx tsc -b --noEmit` проходит без
  ошибок.
- [x] **Сайдбар: разворачивание по гамбургеру + аватар с Gravatar** - три
  мелких пункта бэклога, не входившие в согласованный roadmap, но давно
  замеченные.
  - **Гамбургер-toggle** (`src/components/layout/Sidebar.tsx`) - раньше был
    декоративной кнопкой без обработчика. Теперь переключает свернутый
    (иконки без подписей, `w-16`, как раньше) и развернутый (подписи рядом с
    иконками, `w-56`) режимы; выбор сохраняется в `localStorage`
    (`redmine-sidebar-expanded`) по образцу `ThemeContext` - переживает
    перезагрузку страницы. В развернутом виде `Tooltip` вокруг пунктов не
    рендерится (подпись и так видна, дублировать незачем).
  - **Аватар в Topbar через Gravatar** (`src/components/layout/Topbar.tsx`,
    `src/pages/AppLayout.tsx`) - `getGravatarUrl` из `src/lib/gravatar.ts`
    (был реализован раньше, но не подключен) теперь используется в
    `AvatarImage`; `CurrentUser` расширен полем `email` (`user.mail` из
    `AuthContext`). `d=404` в `getGravatarUrl` заставляет `AvatarImage`
    провалиться и уступить место `AvatarFallback` (инициалы) для
    пользователей без Gravatar - проверено на локальном Redmine (админ без
    Gravatar): запрос на `gravatar.com/avatar/...` корректно уходит и
    получает `404`, в UI остаются инициалы "RA", как и раньше.
  - **Баг найден и починен по пути (не косметика - реальная поломка)**:
    исходный пункт бэклога "поправить отступы между иконками" не
    подтвердился при визуальной проверке (интервалы и так ровные,
    `gap-1`) - зато обнаружилось, что **подсветка активного пункта в
    сайдбаре не работала вообще ни в одном состоянии**, включая
    дефолтное свернутое. Причина - `TooltipTrigger asChild` (Radix Slot)
    оборачивал `NavLink`, а `NavLink.className` был передан функцией
    (`({isActive}) => ...`, стандартный паттерн react-router). Radix Slot
    при мерже пропсов дочернего элемента складывает `className` как
    `[a, b].filter(Boolean).join(" ")`, рассчитывая на строки; className-
    функция молча приводится к строке через `toString()`, и в DOM утекает
    исходный код функции вместо классов - подсветка пропадает без единой
    ошибки в консоли. Фикс - в `Sidebar.tsx` активность пункта теперь
    вычисляется явно через `useLocation()` и локальную `isPathActive()`
    (сам путь или его подстраницы, например `/issues/42` для пункта
    `/issues`), `className` передаётся уже готовой строкой, не функцией -
    Slot-мерж больше не участвует в её вычислении. Проверено в браузере на
    всех трёх реальных маршрутах (`/issues`, `/time`, `/dashboard`) в обоих
    состояниях сайдбара (свернут/развернут) - активный пункт подсвечивается
    красным во всех случаях.
  - Проверено сквозным прогоном в браузере: разворачивание/сворачивание,
    сохранение состояния после `location.reload()`, подсветка активного
    пункта на всех маршрутах в обоих режимах, `AvatarImage`/`AvatarFallback`
    - все ок; `npx tsc -b --noEmit` проходит без ошибок.
- [x] **Страница "Профиль"** (`src/pages/ProfilePage.tsx`, маршрут
  `/profile`, пункт сайдбара активирован в `nav-items.ts`). Данные -
  `GET /my/account.json` (`src/api/account.ts` - `Account`,
  `getMyAccount`/`updateMyAccount`; отдельный модуль от приватных
  `getMyAccount`/`fetchAccount` в `AuthContext.tsx`, которые используются
  только для проверки API-ключа при логине - решили не консолидировать, чтобы
  не рисковать их аккуратно настроенной обработкой сетевых ошибок/401).
  Просмотр - логин (с бейджем "Администратор", если `admin: true`), email,
  имя, фамилия, дата регистрации, дата последнего входа (прочерк, если
  `null`). Правка - только имя/фамилия/email (пароль/язык/уведомления не в
  фокусе, см. "Приоритеты дальше" ниже); "Сохранить" шлёт патч только из
  изменённых полей (`PUT /my/account.json`, по образцу
  `diffFormValues`-паттерна из `IssueDetailPage.tsx`), "Отмена" сбрасывает
  локальный state без запроса. `useAccount` (`src/hooks/useAccount.ts`) -
  тот же паттерн, что `useIssue.ts` (state + `reload()` через счетчик).
  После успешного сохранения помимо локального `reload()` вызывается новый
  `refreshUser()` из `AuthContext` (перечитывает `/my/account.json` и
  обновляет глобального `user`) - без этого имя в Topbar осталось бы старым
  до перезахода. Проверено сквозным прогоном в браузере на локальном
  Redmine: карточка показывает корректные данные, правка фамилии сохраняется
  и сразу отражается в Topbar без перезагрузки страницы, "Отмена" не
  отправляет запрос (значение отката подтверждено в самом Redmine, не только
  в UI), пункт "Профиль" в сайдбаре кликабелен и подсвечивается активным в
  обоих режимах (свернут/развернут).

## Права доступа (permissions)

- [x] Как узнавать права через REST API - исследовано,
  см. [`docs/permissions.md`](docs/permissions.md). Коротко: единого "что мне
  можно" эндпоинта нет, но и резолвить группы вручную не нужно -
  `GET /users/current.json?include=memberships` уже отдает role id по
  каждому проекту с учетом групповых членств, а `GET /roles/{id}.json`
  отдает `permissions: string[]` для роли (кэшировать на сессию, ролей мало).
  `admin: true` с `/my/account.json` обходит все проверки. Все нужные
  эндпоинты уже есть в сгенерированных типах (`src/api/schema.d.ts`).
- [x] **Реализация** - `src/api/permissions.ts` (`fetchPermissions`, см.
  алгоритм в `docs/permissions.md`), подключено в `AuthContext`
  (`projectRoles`/`rolePermissions` грузятся отдельным запросом сразу после
  логина/восстановления сессии, не блокируя сам вход; `can(permission,
  projectId)` - в контексте, `user.admin` обходит проверку). Использовано:
  кнопка "Удалить" на карточке задачи (`delete_issues` - права "только свои"
  не бывает, в отличие от заметок), скрытие "Редактировать"
  (`edit_issues`) там же, скрытие "Добавить задачу" на списке задач
  (`add_issues`) - только когда в Topbar выбран конкретный проект, при "Все
  проекты" право заранее не известно (проект выбирается уже внутри диалога
  создания) - кнопка остается как раньше. Клиентская проверка - это UX, не
  замена серверной: `deleteIssue` (`src/api/issues.ts`) отдельно ловит 403 и
  показывает понятную ошибку на случай гонки (права отозвали между загрузкой
  страницы и кликом). Проверено сквозным прогоном на локальном Redmine
  (docker-compose): у роли "Репортёр" (есть `add_issues`, нет `edit_issues`/
  `delete_issues`) кнопки "Редактировать"/"Удалить" не показываются, у
  админа - показываются, удаление реально прошло (`DELETE /issues/{id}.json`
  → редирект на список).
- [x] Заодно заменили `window.confirm` на нормальную модалку -
  `src/components/ui/alert-dialog.tsx` (добавлен через `npx shadcn add
  alert-dialog`) + обертка `src/components/ConfirmDialog.tsx`. Причина - не
  только эстетика (нативный `confirm()` не следует теме приложения), но и
  то, что блокирующий JS-диалог виснет под браузерной автоматизацией
  (CDP не может кликнуть/сделать скриншот, пока он открыт) - выяснилось на
  практике при сквозной проверке удаления задачи в этой же сессии.
  Использовано для удаления задачи (`IssueDetailPage`) и записи времени
  (`TimeTrackingPage`, ранее тоже был на `window.confirm`).
- [x] Создание задачи при "Все проекты" в Topbar - `IssuesPage` теперь
  фильтрует список проектов, передаваемый в `CreateIssueDialog`, по праву
  `add_issues` (`creatableProjects`); кнопка "Добавить задачу" скрывается,
  если таких проектов нет вообще, а если в Topbar выбран конкретный проект
  без прав - тоже скрывается (не подсовываем создание в другом проекте вместо
  выбранного). Сам `CreateIssueDialog` остался простым - принимает уже
  отфильтрованный список, дополнительно проверяет только что `defaultProjectId`
  входит в этот список, иначе откатывается на первый доступный. Про
  "гейтинг полей внутри формы правки" (смена исполнителя и т.п.) - отдельного
  права на это в Redmine нет, вся форма уже целиком за `edit_issues` (кнопка
  "Редактировать"), закрывать отдельно нечего.
  Проверено сквозным прогоном: пользователь с ролью, где `add_issues` есть
  только на одном из двух проектов - в селекторе диалога виден только
  разрешенный проект, при фильтре по проекту без прав кнопка пропадает
  полностью.

Отмечать пункты по мере выполнения, не оставлять список молча устаревшим.

## Локальный Redmine для тестирования

`docker-compose.redmine.yml` (в репозитории, не в `.gitignore`, но и не часть
фронтового/прокси кода) - одноразовый `redmine:6-alpine` с SQLite, без внешней
БД. `docker compose -f docker-compose.redmine.yml up -d`, затем `admin/admin`
(Redmine попросит сменить пароль при первом входе через веб-UI).

Две вещи, на которые наткнулись при первом поднятии - не баги приложения, а
особенности свежего контейнера:

- **Сид-данные (трекеры/приоритеты/статусы) не подгрузились сами** - обычно
  официальный образ делает это автоматически при первом старте
  (`rake redmine:load_default_data`), но в этом инстансе таблицы остались
  пустыми. Симптом - `POST /issues.json` падает с `422` и текстом "Project
  cannot be blank / Tracker cannot be blank / ..." даже при валидных id (это
  сбивает с толку - выглядит как будто проект/трекер не существуют, а на
  самом деле справочники в принципе пустые). Проверяется отдельными `GET
  /trackers.json`, `/enumerations/issue_priorities.json`,
  `/issue_statuses.json` - если все три отдают `[]`, дело в этом. Чинится
  вручную: `docker exec -e SECRET_KEY_BASE=<любой> -e REDMINE_LANG=ru
  redmine-local bundle exec rake redmine:load_default_data`.
- **Проект, созданный через веб-UI до починки сид-данных, остаётся без
  трекеров** (`"trackers":[]` в `GET /projects/{id}.json`) - нужно донастроить
  `PUT /projects/{id}.json` с `tracker_ids` или через UI (вкладка
  "Настройки" → "Трекеры" проекта), иначе форма создания задачи не даст
  выбрать трекер вообще.

Раз столкнулись с "422 без понятной причины" на создании задачи - в первую
очередь проверять именно это (пустые справочники), а не искать баг в
`CreateIssueDialog`/прокси.

## Приоритеты дальше (сверено с пользователем 2026-08-12)

Сверка через серию уточняющих вопросов ("кто целевой пользователь и что ему
важно") перед тем, как вкладываться в реализацию - см. историю сессии.
Основной вывод: **целевой пользователь - разработчик, который заводит задачи
и трекает время**; анализ/отчётность по ним - вторично. Углубление и
удобство этого сценария приоритетнее охвата остальных модулей Redmine, вплоть
до того, что можно выходить за рамки UI/UX оригинального Redmine (лишь бы
оставаться в рамках его REST API). Это по-прежнему актуальный ориентир при
приоритизации новых задач.

- **Не гнаться за паритетом с нативным Redmine** - вики, Gantt, репозитории
  осознанно не в фокусе. Все силы - в углубление уже реализованных разделов
  (Задачи, Время, Дашборд, карточка задачи), не в расширение вширь.
- **Пункт сайдбара "Цели" удалён** (`src/components/layout/nav-items.ts`) -
  это не сущность Redmine вообще, декоративный пункт исходного Behance-макета,
  под него нет API и придумывать его вручную не стали. "Сообщения" (форумы)
  тоже вырезаны - не в фокусе на разработчике/трекинге времени.
- Актуальное состояние сайдбара (что реализовано, что вырезано и почему) -
  см. "Пустые пункты сайдбара/топбара" ниже, это более свежий и точный
  источник, чем историческая версия этого раздела.
- Все дальнейшие кандидаты на углубление функционала (подзадачи/связи,
  вложения, наблюдатели, лента активности - все реализованы; отчёты,
  нативные Query Redmine, permissions-aware UI и остальное - ещё нет)
  ведутся как **GitHub Issues** в `htonar/redmine-redesign`, не в этом
  файле - см. "Ведение бэклога" ниже.

## Пустые пункты сайдбара/топбара

`src/components/layout/nav-items.ts` - пункты без `path` показаны неактивными
(см. комментарий в файле). В Topbar (`src/components/layout/Topbar.tsx`) были
свои заглушки отдельно от сайдбара - см. ниже.

- [x] **Вырезаны из сайдбара**: "Сообщения" (в REST API Redmine личных
  сообщений нет вообще - только форумы проекта, другая концепция), "Отчеты"
  (нет отдельного REST-ресурса, пришлось бы агрегировать N запросами) и
  "Цели" (в Redmine нет такой сущности - ближайший аналог, версии, не факт
  что то же самое). Решили не гадать, что каждый из них должен значить в
  этом проекте, и убрали пункты вовсе, а не оставили неактивными - см.
  историю обсуждения выше по чату/коммитам, если понадобится вернуться к
  вопросу. `navItems` теперь: Профиль, Учет времени, Активность, Задачи,
  Файлы.
- [x] **Профиль** (`/profile`, `ProfilePage.tsx`) - `GET/PUT
  /my/account.json` (`src/api/account.ts` - `Account`, `getMyAccount`/
  `updateMyAccount`; `useAccount` хук, по образцу `useIssue.ts`). Эта
  реализация и её живое обновление имени в Topbar после сохранения
  (`refreshUser()` в `AuthContext`) - из ветки `feat/issue-attachments` (см.
  чеклист выше, пункт "Страница \"Профиль\"") - там же дубликат этой фичи был
  собран независимо и почти идентично (те же поля, то же решение не
  редактировать язык/уведомления), взяли более полную версию при слиянии
  веток. Редактируем только то, что реально приходит обратно в GET - имя/
  фамилию/email; язык, часовой пояс, `mail_notification`/`pref` **принимает**
  `PUT`, но `GET` их не отдает вообще (это отдельная от User модель настроек,
  в JSON не подмешивается ни в спеке, ни в реальном Redmine) - показывать для
  них форму с заведомо неверными дефолтами было бы хуже, чем не показывать,
  так что раздела уведомлений в профиле нет. Смены пароля тоже нет - в теле
  `updateMyAccount` нет поля password вообще (см. "Важное ограничение:
  авторизация и 2FA" - вход только по API-ключу). И сайдбар, и пункт
  "Профиль" в дропдауне пользователя в Topbar ведут сюда; отдельный пункт
  "Настройки" убрали (дублировал ту же страницу, вел в никуда).
- [x] **Файлы** (`/files`, `FilesPage.tsx`) - список по текущему проекту из
  Topbar (`GET /projects/{id}/files.json`, `src/hooks/useFiles.ts`); если
  проект не выбран ("Все проекты") - список файлов не агрегируется, просьба
  выбрать проект (N запросов на все проекты ради общего списка того не
  стоят). Загрузка - двухэтапная (`src/api/files.ts`,
  `UploadFileDialog.tsx`): сырой `POST /uploads.json` с бинарным телом
  (не JSON - типизированный `client.POST` для этого не годится, добавлен
  `client.rawFetch` в `src/api/client.ts`, который шлет запрос с теми же
  заголовками авторизации/прокси, что и обычный клиент, но без
  принудительного JSON) возвращает `token`, потом обычный JSON `POST
  /projects/{id}/files.json` с этим token. Скачивание - прямая ссылка
  `<a href>` на сам Redmine (не через прокси - обычная навигация не
  подчиняется CORS), с API-ключом в query `?key=` (штатный механизм Redmine
  для ссылок без заголовков). **Важный нюанс, стоивший одного находённого
  сквозным прогоном бага**: `file.content_url` (и аналогично `search.url`,
  см. ниже) приходит от Redmine уже **абсолютным** URL
  (`http://host/attachments/download/...`), а не путем - несмотря на то что
  по названию поля и в спеке ожидался путь; изначальная реализация
  склеивала `baseUrl + content_url` и задваивала адрес, что и поймали при
  проверке. Upload/delete гейтятся правом `manage_files` (см.
  docs/permissions.md).
- [x] **Глобальный поиск в Topbar** (`GlobalSearch.tsx`, использует новый
  `src/components/ui/command.tsx` - добавлен через `npx shadcn add command`,
  тянет `cmdk`) - `GET /search.json` с debounce (300мс,
  `useGlobalSearch.ts`). Результаты-задачи ведут на `/issues/:id` (id
  вытаскивается из `result.url` регуляркой); остальные типы (проект, wiki,
  документ...) - у нас для них нет экранов, открываются на самом Redmine в
  новой вкладке (та же поправка на абсолютный `result.url`, что и у Files
  выше). Машинные значения `type` в ответе поиска не задокументированы в
  REST-вики явно - взяты из практики Redmine, есть fallback на сырое
  значение для незнакомых типов, а не выдуманный перевод.

Все три пункта проверены сквозным прогоном на локальном Redmine
(docker-compose): профиль показывает и сохраняет реальные данные, файл
залился в два шага и скачался с правильным содержимым, поиск находит
задачу и ведет на ее карточку.

## Markdown-редактор и горячие клавиши сохранения

- [x] **Markdown для описания задачи и комментариев** - раньше это были
  голые `Textarea` (описание в `IssueFormFields.tsx`, комментарий в
  `IssueDetailPage.tsx`), значения показывались как `white-space: pre-wrap`
  текст. Теперь:
  - `src/components/markdown/MarkdownContent.tsx` - рендер через
    `react-markdown` + `remark-gfm` (таблицы, чеклисты, зачеркивание),
    стили - `@tailwindcss/typography` (`@plugin "@tailwindcss/typography";`
    в `src/index.css`, классы `prose prose-sm dark:prose-invert`).
    Используется в трех местах: описание задачи (просмотр), тело
    комментария/записи истории (`JournalEntry.tsx`), предпросмотр в самом
    редакторе.
  - `src/components/markdown/MarkdownEditor.tsx` - `Textarea` + тулбар
    (жирный/курсив/код/ссылка/заголовок/список/нумерованный список - каждая
    кнопка оборачивает выделенный текст или вставляет плейсхолдер) +
    переключатель "Предпросмотр"/"Править". Заменяет голый `Textarea` в
    `IssueFormFields` (описание) и в блоке комментария на
    `IssueDetailPage`.
  - **Вставка файлов по Ctrl+V**: `MarkdownEditor` перехватывает
    `onPaste`, если в буфере есть файлы - грузит их сразу через
    `uploadAttachment` (`POST /uploads.json`, тот же двухшаговый флоу, что
    у карточки "Вложения") и вставляет в текст markdown-ссылку по имени
    файла (`![name](name)` для картинок, `[name](name)` для остального).
    Сама по себе загрузка байтов не прикрепляет файл к задаче - только даёт
    `token`; чтобы он не потерялся, вызывающий обязан передать его в
    `uploads` при следующем create/update. Реализовано как паттерн
    "отложенных вложений" в трёх независимых местах с локальным state
    (`pendingUploads`/`pendingDescriptionUploads`/`pendingCommentUploads`):
    `CreateIssueDialog.tsx` (описание новой задачи → `uploads` в
    `POST /issues.json`), `IssueDetailPage.tsx` (правка описания
    существующей задачи → `uploads` в патче `handleSaveEdit`, и отдельно
    комментарий → `uploads` в `handleAddComment`).
  - **Инлайн-картинки в тексте резолвятся на клиенте вручную** - Redmine
    сам резолвит `![](filename)` в приложенный файл с совпадающим именем
    только при своём серверном рендере (Textile/markdown → HTML), а мы
    рендерим markdown сами на клиенте, без готового HTML от сервера. Чтобы
    картинки не были битыми ссылками, `useAttachmentImageUrls.ts`
    подгружает вложения-картинки задачи через уже существующий
    авторизованный blob-fetch (`GET
    /attachments/download/{attachment_id}/{filename}`, `parseAs: "blob"` -
    прямая ссылка `content_url` не понесла бы `X-Redmine-API-Key`, см.
    паттерн из раздела "Вложения к задаче") и подменяет `src` на
    `object URL` по имени файла в кастомном рендерере `img` у
    `react-markdown`. Известное принятое ограничение: пока файл не
    прикреплён к задаче (сразу после Ctrl+V, до сохранения формы),
    картинка в предпросмотре показывается как битая - предпросмотр
    вызывает `MarkdownContent` без списка `attachments`, потому что до
    сохранения official-вложений с сервера ещё нет.
  - Проверено сквозным прогоном в браузере на локальном Redmine:
    форматирование через тулбар, переключение в предпросмотр, вставка
    файла по Ctrl+V (синтетический `ClipboardEvent` с `File`, т.к.
    автоматизация не имеет доступа к реальному системному буферу) -
    `POST /uploads.json` прошёл (`201`), файл появился в тексте markdown-
    ссылкой и после сохранения - как обычное вложение задачи с рабочей
    инлайн-картинкой (`blob:` URL, размеры совпали с исходным файлом).
- [x] **Ctrl+Enter в комментарии - усилено определение нажатия Enter**
  (`MarkdownEditor.tsx`) - пользователь сообщил, что сочетание иногда не
  срабатывает. Не удалось воспроизвести напрямую, но `onKeyDown`
  дополнительно проверяет `e.code === "Enter" || e.code === "NumpadEnter"`
  вместе с `e.key === "Enter"` - `e.code` привязан к физической клавише, а
  не к раскладке/языку ввода, теоретически надёжнее `e.key` в браузерах,
  где тот может отдавать нестандартное значение.
- [x] **Ctrl+S - сохранить форму правки задачи** (`IssueDetailPage.tsx`,
  по прямой просьбе пользователя, как альтернативный путь сохранения рядом
  с Ctrl+Enter у комментария) - `useEffect` с `document.addEventListener
  ("keydown", ...)`, активен только пока `isEditing === true`, вызывает
  `handleSaveEdit()` и обязательно `e.preventDefault()` (иначе браузер
  перехватывает Ctrl+S под нативное "Сохранить страницу"). В отличие от
  хоткея "e" (который явно игнорирует нажатия с фокусом в поле ввода),
  здесь такой проверки нет и не должно быть - сохранять нужно именно когда
  фокус внутри самой формы правки. Добавлена запись в справку горячих
  клавиш (`HotkeysHelpDialog.tsx`, `ISSUE_PAGE_HOTKEYS`). Проверено
  пользователем вручную.

## Бэклог: идеи

Не проработано, только зафиксировано, чтобы не потерять при следующем
планировании.

- [x] **Поиск задач с подсказками - везде, не только в логировании времени**
  - зафиксировано пользователем 2026-08-13 ("отстой какой-то", уточнено -
  имеется в виду обобщить `IssuePicker` из `LogTimeDialog`). `IssuePicker.tsx`
  перенесён `components/time/` → `components/issues/` (был уже полностью
  общим по логике, просто лежал не на своём месте) и подключен во всех трёх
  местах на карточке задачи, где раньше был голый `<Input type="number">` с
  плейсхолдером "№ задачи" без единой подсказки: "Указать родителя",
  "Добавить подзадачу", "Добавить связь" (`IssueDetailPage.tsx`). Состояния
  `parentInput`/`childInput`/`relationInput` сменили тип с `string` на
  `number | null` (раньше руками парсили `Number(x.trim())`, теперь
  `IssuePicker` уже отдаёт число или `null`). Добавлен проп `className` на
  `IssuePicker` (по умолчанию растягивается на всю ширину - годится для
  `LogTimeDialog`, в инлайн-строке на карточке задачи нужна фиксированная
  ширина - `w-56`).
  **Заодно найден и исправлен смежный баг** (зафиксирован пользователем в
  той же сессии): подзадачи нельзя было отвязать - список подзадач показывал
  только ссылку, без кнопки ×, в отличие от родителя и связанных задач (у
  которых удаление уже было). Добавлен `handleRemoveChild` (технически это
  снятие родителя у самой подзадачи - `updateIssue(childId, {parentId:
  null})`, отдельного эндпоинта "убрать подзадачу" в Redmine нет) и кнопка ×
  по образцу связанных задач.
  Проверено сквозным прогоном на локальном Redmine: во всех трёх местах
  поиск по номеру/теме с подсказками работает, добавление подзадачи/связи
  через `IssuePicker` доходит до Redmine, отвязка подзадачи по × работает и
  отражается в истории изменений.
- [x] **Markdown-редактор для описания/комментариев + вставка файлов по
  Ctrl+V** - зафиксировано пользователем 2026-08-13, реализовано в этой же
  сессии. См. раздел "Markdown-редактор и горячие клавиши сохранения" выше -
  свой лёгкий редактор (`MarkdownEditor.tsx`, тулбар + `react-markdown` +
  `remark-gfm`) вместо стороннего пакета, вставленный по Ctrl+V файл
  становится настоящим вложением задачи (не текст-в-описании) через тот же
  `uploadAttachment`, что и секция "Вложения" - согласовано с ней, не
  дублирует.
- [x] **Канбан-доска для задач** - переключатель вида (иконки таблица/канбан)
  в панели фильтров `IssuesPage` (`src/pages/IssuesPage.tsx`, состояние
  `layout: "table" | "kanban"` - не `view`, это имя занято переменной цикла
  сохраненных видов). Канбан работает только с выбранным проектом в Topbar
  (статусы/workflow не глобальны для "всех проектов") - при `selectedProjectId
  === null` вместо доски показывается подсказка выбрать проект, без попытки
  молча рисовать пустые колонки. Реализация - `src/components/issues/KanbanBoard.tsx`
  + `src/hooks/useIssueStatuses.ts` (справочник `GET /issue_statuses.json` -
  колонки) + `src/hooks/useKanbanIssues.ts` (задачи проекта, всегда
  `status: "all"` - статус кодируется колонкой, не фильтром; один запрос на
  лимит 100 без постраничной подгрузки, доска не тянет колонки страницами;
  `hasMore` предупреждает, если задач больше 100). Драг-н-дроп -
  `@dnd-kit/core` + `@dnd-kit/utilities` (новая зависимость, активно
  поддерживается и с a11y из коробки, в отличие от заброшенного
  `react-beautiful-dnd`). Карточка перетаскивается только за ручку
  (`GripVertical`), не всей площадью - иначе клик "открыть задачу" и начало
  драга конфликтуют. Перетаскивание в другую колонку = смена статуса, тот же
  `updateIssue(client, id, { statusId })`, что и `Select` в
  `IssueDetailPage` - новой логики на бэкенде/прокси не понадобилось.
  Доступные переходы статуса заранее не проверяются (workflow зависит от
  роли/трекера/текущего статуса, см. `allowed_statuses`) - было бы N+1
  запросов ради подсветки; вместо этого карточка переносится оптимистично и
  откатывается обратно, если Redmine отклонил переход. Проверено сквозным
  прогоном на локальном Redmine: переключение таблица/канбан, подсказка при
  "все проекты", отрисовка колонок по всем статусам, перетаскивание карточки
  между колонками с реальным сохранением статуса (проверено повторным `GET
  /issues/{id}.json`), переход на карточку задачи по клику.
- [x] **Горячие клавиши** - по образцу Linear/Gmail, свой хук
  `src/hooks/useGlobalHotkeys.ts` (не тащили `react-hotkeys-hook` - логика
  простая, свой `keydown`-листener на `document` с проверкой, что фокус не в
  поле ввода/textarea/select/contenteditable и не открыт диалог
  (`[data-slot="dialog-content"]`, атрибут shadcn Dialog), иначе `c` ловился
  бы посреди набора текста). `g` работает как префикс двухклавишной
  последовательности (`g`+`i`/`g`+`d`/`g`+`t` - переход в Задачи/Дашборд/Учет
  времени) с таймаутом 800мс. Глобальные хоткеи монтируются один раз в
  `AppLayout.tsx` (`c` - открыть `CreateIssueDialog`, `/` - фокус в
  `GlobalSearch`, `?` - подсказка по хоткеям); `e` (редактировать открытую
  задачу) и `Ctrl/Cmd+Enter` (отправить комментарий) - локальные, только на
  `IssueDetailPage.tsx`. Подсказка по хоткеям -
  `src/components/layout/HotkeysHelpDialog.tsx`, открывается по `?` и по
  кнопке-клавиатуре в Topbar (без такой кнопки хоткеи никак не обнаружить,
  кроме как узнать о них из этого файла).
  **Два реальных бага найдены и починены по пути** (оба - не в новом коде
  хоткеев, а в существующем `CreateIssueDialog`, тестами до этого не
  ловились, потому что раньше диалог открывался только через `DialogTrigger`,
  никогда не управлялся снаружи):
  1. Чтобы навесить хоткей `c` на уже существующий `CreateIssueDialog`
     (раньше - только `trigger` + внутренний `open` state), диалог получил
     опциональные `open`/`onOpenChange` пропы (управляемый режим, `trigger`
     стал опциональным) - для хоткея `AppLayout` монтирует отдельный
     `CreateIssueDialog` без `trigger`. При этом сброс формы (проект по
     умолчанию, пустые поля) раньше жил в `handleOpenChange`, которую Radix
     вызывает только для *своих* переходов (клик по оверлею/Esc/триггер) - а
     не когда `open` меняется снаружи. Хоткей `c` открывал диалог с пустым
     полем "Проект" вместо дефолтного, хотя обычный клик по кнопке "Добавить
     задачу" работал верно - разница проявлялась только при внешнем
     управлении `open`. Пойман сквозным прогоном в браузере (JS
     `dispatchEvent(KeyboardEvent)` + проверка DOM), не по коду. Фикс - логика
     сброса вынесена из `handleOpenChange` в `useEffect` по `[open]`, который
     срабатывает независимо от того, что вызвало открытие.
  2. Проверка результата хоткея сразу после `dispatchEvent` синхронно
     возвращала "ничего не произошло" - оказалось не багом приложения, а
     артефактом теста (React не применяет обновление DOM синхронно в том же
     тике после события, инициированного вне React) - подтверждено `await
     setTimeout(100)` перед проверкой; реальные нажатия клавиш пользователем
     этой проблеме не подвержены.
  Также попутно поймано (не баг, но важный нюанс тестирования): `id`,
  переданный в `CommandInput` (shadcn/cmdk) для фокуса поиска по `/`,
  молча перезаписывается автосгенерированным Radix id внутри самого cmdk -
  для поиска элемента снаружи нужно использовать `[data-slot="command-input"]`
  (наш собственный атрибут, cmdk его не трогает), не `id`.
  Проверено сквозным прогоном в браузере на локальном Redmine: `g`+`i`/`g`+`d`/
  `g`+`t` переводят между разделами, `/` фокусирует поиск, `?` и кнопка в
  Topbar открывают подсказку (текст "Ctrl + Enter" показан как одновременное
  нажатие, не как последовательность - отдельно проверено и поправлено),
  `c` открывает диалог создания с верно подставленным проектом по умолчанию,
  задача создана и открыта; на карточке задачи `e` включает режим правки,
  `Ctrl+Enter` в поле комментария отправляет его (проверено появлением в
  истории и очисткой поля). Хоткеи не срабатывают при фокусе в полях ввода
  и при открытом диалоге - тоже проверено вручную.
- [x] **Мини-виджет "долг по трудозатратам за неделю"**
  (`WeeklyTimeDebtWidget.tsx`, `useWeeklyTimeDebt.ts`) - на `DashboardPage` и
  `TimeTrackingPage` сверху. Будние дни текущей недели (`GET
  /time_entries.json?spent_on=w`, тот же шорткат, что у фильтра "Эта неделя")
  как мини-столбики факт/норма (8ч), а не голые цифры - см. dataviz-скилл:
  форма "Δ к цели" на статус-цветах (`--success`/новый `--warning` в
  `index.css`, залит только цвет статуса - каждая полоска дополнительно
  подписана часами и днем недели, а не одним цветом). "Осталось затрекать" -
  крупным числом сверху, нейтральным текстом + отдельная цветная иконка
  рядом (не текст цветом - см. dataviz-скилл, "text never wears the data
  color"; изначальная версия красила сам текст, поймано валидатором
  `validate_palette.js` из скилла - contrast 2.03/2.15 не проходил даже
  относаксed-порог для крупного текста). Клик "+" под недотрекованным днем
  открывает `LogTimeDialog` с `defaultSpentOn` (новый проп, только для
  создания - в режиме правки дата остается из `initial`).
  **По пути найдены и починены три реальных бага** (не в новом коде - в уже
  существующем `LogTimeDialog`, просто раньше никто не пробовал ввести
  круглое число часов через реальный клик в браузере):
  1. `<Input type="number" min={0.01} step={0.25}>` у поля "Часы" - HTML
     constraint validation считает шагом `min + n*step`, т.е. валидны только
     0.01, 0.26, ..., 4.76, 5.01 - **почти любое круглое число (5, 8...)
     браузер тихо отклонял**, submit не срабатывал вообще без единого
     сообщения об ошибке в UI (`form.requestSubmit()` показывает нативный
     тултип, обычный `.click()` в headless-браузере - нет, просто ничего не
     происходит). Фикс - `min={0}` (шаг с нуля включает все круглые числа),
     ноль/отрицательные по-прежнему отсекает JS-проверка после submit.
  2. Поле "№ задачи" было голым `<input type=number>` без единой подсказки -
     пользователь (в этой же сессии) отдельно пожаловался, что вводить номер
     "вслепую" неудобно. Заменено на `IssuePicker.tsx` - комбобокс с
     подсказками (`useIssueSearch.ts`): чисто числовой ввод отдельно
     проверяется через `getIssueSummary` (подтверждает, что номер существует,
     и сразу показывает настоящую тему - не только красивее, но и
     подстраховка от опечатки в номере), плюс полнотекстовый поиск по теме
     через `GET /search.json?issues=1` (`searchIssues` в `api/search.ts`,
     опционально в рамках одного проекта через `/projects/{id}/search.json`).
     Выбранная задача - чип "#id Тема" с крестиком, не голый номер. Task
     остаётся необязательным (можно вместо него выбрать проект) - это
     осознанное существующее поведение Redmine (лог времени без привязки к
     конкретной задаче - легитимный сценарий), не трогали.
  3. В `useIssueSearch`/`useGlobalSearch`-стиле debounce был общий порог
     "искать от 2 символов" - для текста разумно, но резал поиск по
     однозначным номерам задач (задача `#1` не находилась, пока не наберешь
     вторую цифру). Порог теперь 1 символ для чисто цифрового ввода, 2 - для
     текстового.
  Все три бага пойманы и проверены исключительно сквозным прогоном в
  браузере на локальном Redmine - `npm run build`/`lint` их не ловят
  (типы и линт были чистыми на всех трёх багах).
- [x] **Шаблоны задач по типам** - чисто клиентская функция (у Redmine нет
  сущности "шаблон задачи" в REST API), по образцу уже реализованных
  "сохраненных видов" - `src/lib/issue-templates-storage.ts` +
  `src/hooks/useIssueTemplates.ts` (localStorage, ключ по инстансу+
  пользователю, как у `issue-views-storage.ts`). Шаблон = имя + трекер/
  приоритет/категория/описание (`Omit<IssueTemplate, "id">`) - не путать с
  трекером Redmine (Bug/Feature/Support) - шаблон это конкретный
  преднастроенный набор полей поверх трекера, разных шаблонов на один
  трекер может быть несколько ("Баг - фронтенд" / "Баг - бэкенд" и т.п.).
  **Три дефолтных шаблона подставляются один раз при первом обращении**
  (`seedDefaults` в `issue-templates-storage.ts`) - "Баг"/"Улучшение"/
  "Задача", у каждого только скелет описания (заголовки markdown, чеклист),
  трекер/приоритет/категория - `null` (id трекеров/приоритетов не переносимы
  между инстансами Redmine - разные наборы, разная локализация названий;
  безопасно предзаполнить можно только текст). Список не пустой с первого
  открытия, дальше ведет себя как обычные пользовательские записи (в т.ч.
  дефолты можно удалить).
  UI - в `CreateIssueDialog.tsx`: **не** отдельное поле формы (после
  прогона в браузере пользователь отметил, что и так плотная модалка
  создания задачи становится перегруженной) - компактная кнопка "Шаблон" в
  шапке диалога (`DropdownMenu`, по образцу дропдауна "Виды" на
  `IssuesPage`), применяет шаблон через `updateField` по выбору пункта,
  удаление - иконка корзины у каждого пункта (как у "Видов"), сохранение
  текущих трекер/приоритет/категорию/описание - пункт "Сохранить текущее как
  шаблон..." в том же меню, открывает `SaveTemplateDialog.tsx` (диалог
  поверх диалога - по образцу `SaveViewDialog.tsx`, отдельный компонент, не
  обобщение - копия текста другая, обобщать через параметризацию не стали).
  Кто не хочет шаблон - просто не открывает меню, форма остается такой же
  пустой, как раньше (это было отдельным вопросом пользователя - шаблон
  всегда необязателен, дефолт формы не завязан на них).
  **Баг найден и починен по пути**: DialogContent из shadcn держит
  встроенную кнопку закрытия `×` абсолютным позиционированием
  (`absolute top-2 right-2`, ~28px), которое не учитывается обычным потоком
  контента - шапка с кнопкой "Шаблон" вплотную к правому краю налезала на
  `×` (кнопки визуально сливались/перекрывались - поймано только по
  скриншоту, не по коду). Фикс - `pr-8` на строке с заголовком+кнопкой,
  чтобы гарантированно освobождать место под встроенный `×`.
  Проверено сквозным прогоном в браузере на локальном Redmine: все три
  дефолтных шаблона видны при первом открытии диалога без единой ручной
  настройки, применение шаблона (Баг) подставляет описание-скелет,
  сохранение текущих полей как нового шаблона (с трекером/приоритетом)
  появляется в том же меню и переживает переоткрытие диалога, удаление
  шаблона убирает его из списка, создание задачи с примененным шаблоном
  реально доходит до Redmine и открывает карточку созданной задачи;
  вложенный `SaveTemplateDialog` открывается/закрывается поверх основного
  диалога без потери состояния последнего (Escape закрывает только
  вложенный уровень, не всю форму).
- [x] **Мягкая геймификация** - декоративный слой поверх обычных состояний
  (`status.is_closed`, `done_ratio`), новых данных не потребовалось. Не
  игровая механика с очками/уровнями (в Redmine для этого все равно нет
  данных вроде streak'ов вне API) - только анимации/мелкие приятности.
  - **Confetti** (`canvas-confetti`, новая легкая зависимость - не тащили
    framer-motion/целый anim-фреймворк ради одного эффекта) -
    `src/lib/celebrate.ts`, `celebrate()` уважает `prefers-reduced-motion`
    через встроенную опцию библиотеки `disableForReducedMotion`. Вызывается
    в трёх местах, только на реальном переходе "не закрыта → закрыта" (не на
    каждой смене статуса и не при загрузке уже закрытой задачи - иначе
    залп срабатывал бы на каждый визит страницы):
    1. `IssueDetailPage.handleStatusChange` - смена статуса на `is_closed`
       через Select в шапке карточки.
    2. `IssueDetailPage.handleSaveEdit` - выход на `done_ratio: 100` через
       форму правки (независимо от статуса).
    3. `KanbanBoard.handleDragEnd` - перетаскивание карточки в закрывающую
       колонку.
  - **Плавный прогресс-бар** - `src/components/ui/progress.tsx`, был
    `transition-all` без явной длительности (дефолт Tailwind 150мс,
    практически незаметно на заполнении с нуля) - `duration-700 ease-out`,
    `motion-reduce:transition-none` для тех, кто попросил браузer уменьшить
    анимации.
  - **Похвала при закрытии долга по трудозатратам** - оказалось уже
    реализовано в `WeeklyTimeDebtWidget.tsx` в предыдущей сессии (`totalDeficit
    === 0` → "Всё затрекано" + иконка `Check`, вместо сухого "0ч") - не
    переделывали.
  - **Hover/tap-микроанимации на карточках канбана** - `KanbanBoard.tsx`,
    `KanbanCard`: `hover:-translate-y-0.5 hover:shadow-md` с
    `transition-[transform,box-shadow]`, `motion-reduce:hover:translate-y-0`
    для reduced-motion.
  Проверено сквозным прогоном на локальном Redmine: смена статуса на
  "Closed" создаёт canvas-элемент confetti (поймано через `MutationObserver`
  в браузере - конфетти живёт доли секунды, не ловится обычным скриншотом),
  статус сохраняется корректно; hover-эффект карточки канбана и анимация
  прогресс-бара проверены визуально.
- [x] **Пользовательские поля (custom fields)** - зафиксировано пользователем
  2026-08-13: на его рабочем Redmine есть кастомные поля в задачах, приложение
  их не читало и не показывало вообще.
  - **Чтение** - `custom_fields` уже приходит на самом issue без отдельного
    `include` (в отличие от attachments/watchers) - показывается как
    read-only `Field` в метаданных карточки (`IssueDetailPage.tsx`),
    `Array.isArray` для полей с `multiple`.
  - **Определения полей** (тип, `possible_values`, к каким трекерам
    применимо) - `GET /custom_fields.json`
    (`src/api/customFields.ts`, `useCustomFieldDefinitions.ts`) - **только
    для админов** в реальном Redmine (не задокументировано явно в REST-вики,
    подтверждено практикой - обычный пользователь получает 403). Поэтому
    best-effort: любая неудача → `[]`, без ошибки. Для не-админов кастомные
    поля всё равно читаются/редактируются в форме (значения уже есть на
    issue) - просто обычным текстовым инпутом вместо типового
    (select для list/bool и т.п.), это честный fallback, не выдумываем тип.
  - **Форма создания/правки** (`IssueFormFields.tsx`, `CustomFieldFormValue`
    + `CustomFieldInput`) - рендер по `field_format`: `bool` → Select Да/Нет,
    `enumeration`/`list` с `possible_values` → Select, `date`/`int`/`float`
    → типизированный `Input`, `text` → `Textarea`, `multiple` → текст через
    запятую (MVP, не отдельный multi-select компонент - осознанное
    упрощение), остальное → обычный текст.
  - **Применимость поля к трекеру при создании** (`CreateIssueDialog.tsx`) -
    **не полагаемся на `d.is_for_all`**: реальный Redmine (проверено на
    локальном инстансе через rails console) вообще не отдаёт этот ключ в
    JSON, ни истинный, ни ложный - похоже на баг сериализации в самом
    Redmine. Вместо этого - `d.trackers?.some(t => t.id === selectedTracker)`
    строго. **Экспериментально подтверждено** (rails console,
    `Issue#available_custom_fields`): пустой `trackers` означает "поле не
    применимо ни к одному трекеру", а НЕ "применимо ко всем" - даже у поля с
    `is_for_all=true` в БД. `is_for_all` - отдельная ось (не требовать
    привязку к каждому ПРОЕКТУ по отдельности), не отменяет привязку к
    трекеру. Первая версия фильтра ошибочно трактовала пустой `trackers` как
    "глобальное поле" - поймано и исправлено сквозным прогоном (значение
    поля с `is_for_all=true` без привязки к трекеру молча не сохранялось).
  - **Запись** - `custom_fields: {id, value}[]` в теле `POST`/`PUT`
    (`src/api/issues.ts`, `IssueFieldsInput.customFields`) - формат
    подтверждён рабочим сквозным прогоном через curl напрямую к Redmine.
  - **История изменений** (`JournalEntry.tsx`) - записи об изменении custom
    field приходят с `property: "cf"`, `name` = id поля строкой (не имя!) -
    без карты id→имя показывало бы голый номер поля. Карта строится в
    `IssueDetailPage.tsx` из `issue.custom_fields` (не требует прав админа,
    основной источник) и дополняется `customFieldDefinitions` (для полей,
    которых на текущей версии issue уже нет, но которые встречаются в старой
    истории). В `ActivityFeed` на дашборде карта не прокидывается (N+1 ради
    ленты не оправдан) - там такая запись покажет `Поле #N`, это ожидаемый
    fallback, не баг.
  - **Не реализовано осознанно**: настоящий multi-select виджет (текст через
    запятую вместо него), поля типа `user`/`version` без выделенного
    select-компонента (обычный текст - ожидается id или точное значение).
  **Баг найден и починен позже (2026-08-13)**: read-only отображение bool-поля
  на карточке (вне режима правки) показывало сырое значение Redmine ("1"/"0")
  вместо "Да"/"Нет" - пользователь заметил на "Срочно?". Добавлен
  `formatCustomFieldValue()` - при наличии `customFieldDefinitions`
  форматирует `bool` в "Да"/"Нет", а `list`/`enumeration` берёт `label` из
  `possible_values` вместо сырого `value` (если они различаются). Без
  определений (не-админ) - значение как есть, честный fallback.
  Проверено сквозным прогоном на локальном Redmine (3 кастомных поля -
  list/bool/string, созданы через rails console, т.к. REST API не создаёт
  custom fields): все поля появляются в форме создания при выборе трекера,
  сохраняются и корректно читаются с карточки (bool - "Да"/"Нет", не "1"/"0"),
  редактирование существующей задачи показывает типовой Select для list/bool,
  история изменений показывает имя поля, а не номер.
- [x] **Скроллбары не в стиле проекта** - `src/index.css`, `@layer base`.
  Firefox - `scrollbar-width: thin` + `scrollbar-color: var(--border)
  transparent` на `html` (оба свойства наследуемые - действуют на все
  прокручиваемые контейнеры страницы, не только на body, без точечной
  расстановки по компонентам). Chromium/Safari -
  `*::-webkit-scrollbar`/`-track`/`-thumb`/`-thumb:hover` (10px, скругленный
  thumb, `border` + `background-clip: padding-box` для отступа от края;
  `::-webkit-scrollbar` не наследуется, поэтому селектор универсальный, а не
  только на `html`/`body`). Цвета - существующие токены темы (`--border` в
  покое, `--muted-foreground` на hover), поэтому переключаются вместе со
  светлой/тёмной темой автоматически, без дублирования palette. Проверено
  визуально (zoom-скриншот) в обеих темах на скролле внутри диалога
  (`max-h-[60vh] overflow-y-auto` в `CreateIssueDialog`) - тонкий
  скроллбар в тон темы вместо нативного серого/белого ОС.
- [x]/[ ] **Местами верстка отвратительна** - зафиксировано пользователем
  2026-08-13, первый конкретный пример уточнён отдельно и исправлен (см.
  ниже); остальное без указания экранов - открытый пункт.
  - **[x] Высота канбан-доски зависела от содержимого** - конкретный пример
    от пользователя. `KanbanBoard.tsx`: раньше колонки были `flex-col` без
    ограничения высоты - при разном числе карточек в статусах колонки
    получались визуально рваными (хотя сами по себе вытягивались в
    одинаковую высоту через дефолтный flex `align-items: stretch`, общая
    высота доски "гуляла" вместе с самой заполненной колонкой - с малым
    числом карточек смотрелось нормально, с большим or неравномерным
    распределением по статусам - плохо, лишняя пустота под короткими
    колонками растягивалась на всю высоту самой длинной). Фикс -
    фиксированная высота доски `h-[70vh]` (не `max-h` - именно
    фиксированная, не зависящая от контента), каждая колонка скроллится по
    вертикали независимо (`overflow-y-auto` на списке карточек, `min-h-0`
    чтобы flex-контейнер разрешил себе сжаться до этой высоты - иначе
    overflow не сработает), шапка колонки (бейдж статуса + счетчик) остаётся
    закреплённой (`shrink-0`).
    **Заодно найден и исправлен смежный баг**, пойманный пользователем в
    процессе ("колесико мыши там не работает, приходится чисто за скроллбар
    тянуть") - `overflow-x-auto` контейнеры по умолчанию не реагируют на
    вертикальное колесо мыши, только на перетаскивание самого скроллбара.
    Добавлен `onWheel` на доску: переводит вертикальный wheel в
    горизонтальный скролл доски, но **только** если курсор не над списком
    карточек колонки, у которого реально есть что скроллить вертикально
    (`scrollHeight > clientHeight`, помечен `data-kanban-column-scroll`) -
    иначе колесо мыши над длinnой колонкой листало бы доску влево-вправо
    вместо скролла самой колонки вниз. Проверено сквозным прогоном в
    браузере (создано 15 дополнительных задач в один статус): все колонки
    одной высоты независимо от числа карточек в них, длинная колонка
    скроллится колесом мыши вертикально не задевая соседей, короткие
    колонки (в т.ч. пустые) при наведении отдают колесо мыши доске -
    доска скроллится горизонтально, drag-n-drop между колонками по-прежнему
    работает корректно.
  - **[x] Остальное без конкретики** - закрыто отдельным жёстким аудитом,
    см. раздел ниже.

## Жёсткий UI/UX-аудит (2026-08-13)

Обязательный шаг перед паковкой/CI/CD (см. пункт в бэклоге ниже) - пройден.
Поднят одноразовый локальный Redmine (`docker-compose.redmine.yml`) с демо-
проектом и 5 задачами вперемешку по трекерам/статусам/`done_ratio`, пройдены
Дашборд, список задач (таблица + канбан), карточка задачи (просмотр + правка),
Учет времени, Файлы, диалог создания задачи, оба состояния сайдбара, обе темы.

Найдено и исправлено:

- **Прогресс-бар "Готовность" был не виден на 0%** (`src/components/ui/progress.tsx`)
  - пустая дорожка красилась в `bg-muted`, который в этой палитре почти не
  отличим от фона карточки (`#1c2545` на `#161f38` в темной теме, аналогично
  в светлой) - подтверждено замером `getComputedStyle` до и после фикса, не
  только на глаз (в сжатых скриншотах разница вообще стиралась). На 100% бар
  был виден (сплошная заливка primary), а вот на 0%/малых значениях -
  никакого намека на дорожку, только число справа. Похожая проблема на самом
  деле касалась и части `#e8eaf0`-на-`#ffffff` в светлой теме. Фикс - дорожка
  теперь `bg-foreground/10`, тот же прием, что уже используется для рамки
  самого `Card` (`ring-foreground/10`) - гарантированно виден поверх любого
  фона в обеих темах, не завязан на то, насколько близки соседние токены
  палитры.
- **Канбан-карточка не показывала трекер** (`src/components/issues/KanbanBoard.tsx`)
  - тот же баг, что раньше был найден и починен в табличном виде списка
  задач ("не указывается тип - баг или обычная таска"), но фикс тогда не
  распространили на канбан-карточку - она показывала только приоритет и
  исполнителя. Добавлено `#id · Трекер` в шапку карточки, рядом с номером
  (не отдельным бейджем - карточка и так плотная).
- **Плашка приоритета в шапке карточки задачи была на 12px ниже соседних
  кнопок** (`src/pages/IssueDetailPage.tsx`) - `Badge` по умолчанию `h-5`
  (20px), а "Редактировать"/"Удалить"/статус-`Select` рядом - `h-8` (32px);
  на замере `getBoundingClientRect` разница подтвердилась, не показалось.
  Из-за этого ряд элементов в шапке выглядел рвано - один явно мельче
  остальных, будто случайно уменьшенный. Выровнено под общую высоту (`h-8
  px-3 text-sm` на самой плашке), стиль оставлен "бейджевым" (не кнопка -
  на эту плашку нет действия по клику, в отличие от соседей).

Проверено сквозным прогоном в браузере (не только `npm run build`/`lint`,
хотя оба тоже чистые): прогресс-бар виден на 0% и на промежуточных
значениях в обеих темах, канбан-карточка показывает трекер, плашка
приоритета вровень с кнопками и селектом статуса.

Не найдено проблем (проверено целенаправленно, не просто "не бросилось в
глаза"): создание задачи, форма правки, учет времени, файлы - плотность и
высота полей единообразны, gigantism/мелкий текст не обнаружены. Выбор
проекта в Topbar корректно переживает клиентскую навигацию между разделами
(изначально показалось багом - "сбрасывается на каждой странице", но это
был артефакт тестирования полной перезагрузкой страницы через адресную
строку вместо клика по сайдбару; при обычной SPA-навигации состояние
общее и не теряется).

## Тёмная тема: точечный аудит (2026-08-13, issue #10)

Отдельный проход тем же методом, что "Жёсткий UI/UX-аудит" выше, но нацеленный
конкретно на тёмную тему (GitHub issue #10) - на уже поднятом локальном
Redmine/dev-сервере пройдены Задачи (таблица + канбан), карточка задачи
(метаданные, подзадачи/связи, вложения, наблюдатели, время, история/
комментарии), Учет времени, Дашборд, Профиль, Файлы - всё в тёмной теме.

Найден и исправлен один баг того же класса, что уже чинили дважды раньше
(`Progress` на 0%, см. выше) - низкоопacity цветные подложки почти сливаются
с фоном карточки в этой палитре:

- **Дорожка столбиков в `WeeklyTimeDebtWidget`** (`src/components/time/WeeklyTimeDebtWidget.tsx`,
  виджет "Трудозатраты на этой неделе" на `/time` и `/dashboard`) - фон
  столбика-дня красился в `bg-muted/60` (будущие дни) или `bg-success/15`/
  `bg-warning/15` (прошедшие дни), оба варианта практически невидимы на
  фоне карточки (`--card: #161f38`) в темной теме - на дне без залогированных
  часов от столбика оставался только едва различимый прямоугольник, о его
  существовании говорила только подпись "0.0ч" рядом. Подтверждено
  zoom-скриншотом. Фикс - тот же прием, что уже применялся к `Progress`
  (`bg-foreground/10` для дорожки, независимо от future/success/warning;
  для будущих дней поверх добавлен `opacity-50`, чтобы отличать их от
  прошедших без цветовой семантики).

Проверено: zoom-скриншот столбиков на `/time` до и после фикса (дорожка стала
чётко видна на 0ч), тот же виджет на `/dashboard` (тот же компонент, тот же
фикс), `npm run build` и `npm run lint` чистые (без новых ошибок/варнингов
сверх уже существующих, не связанных с этим изменением).

Остальные пройденные экраны - без замечаний (в отличие от предыдущего общего
аудита, здесь новых проблем, кроме указанной, не обнаружено).

## Бэклог: паковка в десктоп-приложение + CI/CD

Сейчас единственный способ запустить всё вместе локально - Docker
(`docker-compose.yml`, см. выше). Цель - десктопный дистрибутив (Win/macOS/
Linux), чтобы не поднимать три контейнера каждый раз ради обычного
использования. Ничего не реализовано, только направление:

- [x] **Обязательный шаг перед стартом этого трека: жёсткий UI/UX-аудит**
  (зафиксировано пользователем 2026-08-13 явно как "перед тем, как делать
  релизный механизм с CI/CD") - **пройден**, см. раздел "Жёсткий UI/UX-аудит
  (2026-08-13)" выше. Гигантизма/слишком мелких элементов при целевом
  проходе не найдено; три реальных верстки-бага найдены и починены
  (невидимый на 0% прогресс-бар, канбан-карточка без трекера, разъехавшаяся
  по высоте плашка приоритета).
- [x] **Выбор обертки: Electron vs Tauri** - решено в пользу **Tauri**
  (сверено с пользователем 2026-08-13, основной довод - вес дистрибутива:
  Electron бандлит Chromium+Node, лишние ~100+ МБ ни за что при живом
  системном WebView).
- [x] **Прокси при паковке - не нужен вообще, не sidecar** (пересмотрено
  относительно исходной формулировки этого пункта - см. историю сессии).
  Изначальная идея была "тащить Node-прокси сайдкаром" (`bundle.externalBin`),
  но это возвращало вес, от которого уходили выбором Tauri - сам бинарник
  Node в этой среде **117 МБ** (`node --version` → `du -sh $(which node)`),
  почти как весь выигрыш от Tauri против Electron. Вместо этого - решение
  на уровне архитектуры: **CORS - ограничение браузера/webview на JS-уровне,
  Rust на него не натыкается вообще** (`reqwest` делает обычный исходящий
  HTTP-запрос, никакого понятия CORS у него нет). Значит для десктоп-сборки
  прокси-сервер просто не нужен - весь смысл прокси (`server/`, см. "CORS и
  прокси-бэкенд" выше) был в том, что запрос шел из `fetch()` внутри
  браузера; в десктоп-сборке запрос вместо этого идет из Rust-команды.
  - **Реализация**: `src-tauri/src/proxy.rs` - Tauri-команда `proxy_request`
    (async, на `reqwest` с `rustls-tls` - без системной OpenSSL, кросс-
    платформенно без лишних зависимостей на сборочной машине), форвардит
    запрос 1:1 (метод, путь-в-URL, отобранные заголовки, тело) - тот же
    контракт, что у `/proxy/*` в `server/src/index.ts`, тот же список
    форвардимых заголовков (`FORWARDED_REQUEST_HEADERS`). Тело - `base64` в
    обе стороны (`invoke()` умеет только JSON-совместимые значения, а
    вложения/картинки бинарны) - `src/api/tauriFetch.ts` кодирует/декодирует
    на границе, чанками по 0x8000 байт (не `String.fromCharCode(...bytes)`
    на весь файл разом - переполняет стек вызовов на больших файлах).
  - **`ALLOWED_REDMINE_HOSTS` в десктоп-сценарии не нужен вообще** (снимает
    исходный открытый вопрос этого пункта) - тот существовал в Node-прокси
    как защита от SSRF: прокси слушает сетевой порт, и без allowlist его
    может дернуть любой сайт в интернете от имени пользователя. Tauri IPC
    (`invoke`) не слушает сеть и недоступен снаружи приложения - вызвать
    команду может только код самого webview, тот же trust boundary, что и у
    остального приложения (тот же принцип, что уже применялся к отказу от
    `window.confirm` в пользу `AlertDialog` - что доступно только изнутри
    доверенного контекста, не нуждается в защите от внешнего мира).
  - **Интеграция во фронт** - `src/api/client.ts`: `isTauri()` (`@tauri-apps/api/core`)
    определяет режим при создании клиента; в Tauri-режиме `fetchBaseUrl`
    указывает прямо на `baseUrl` (реальный Redmine, не на прокси), заголовок
    `X-Redmine-Target` не нужен (адрес цели и так уже в URL запроса), и в
    `createClient()` передается кастомный `fetch: tauriFetch` -
    `openapi-fetch` поддерживает это из коробки (`fetch?: (input: Request) =>
    Promise<Response>` в его типах) - сам факт, что клиент можно
    инициализировать с любым fetch-совместимым обработчиком, и позволил
    подключить Tauri-режим без единого другого вызывающего места в коде.
    `rawFetch` (сырые запросы вроде `POST /uploads.json`, см. "Вложения к
    задаче") заведен через тот же `tauriFetch` при `tauri === true`.
  - Прокси-сервер (`server/`) **не тронут** - веб/Docker-сборка как была, так
    и работает через него; ветвление только в `client.ts`.
- [x] **Windows - код кроссплатформенный с самого начала** (явно
  зафиксировано пользователем 2026-08-13 - не откладывать до "когда-нибудь
  потом"), при этом реальная сборка/тест под Windows - не сегодня (нет
  доступной машины/CI в этой сессии). Что сделано именно сегодня: `reqwest`
  с `rustls-tls` вместо `native-tls` (не тянет системную OpenSSL, которой на
  Windows нет по умолчанию), `tauri.conf.json` → `bundle.targets: "all"`
  (включает `nsis` для Windows наравне с `deb`/`appimage`/`dmg`, не только
  Linux), никаких Unix-специфичных путей/предположений в
  `src-tauri/src/proxy.rs` (обычный `reqwest`, ничего платформенного).
  Настоящая сборка под Windows и тонкости вроде WebView2 runtime - в рамках
  пункта "GitHub Actions" ниже, когда дойдет очередь до матрицы.
- [x] **Скаффолдинг и сквозная проверка (2026-08-13)** - `src-tauri/`
  (`npx tauri init`), продукт **"Redfine"** (имя дал пользователь), bundle id
  `com.htonar.redfine`. Иконка - сгенерирована (`npx tauri icon`) из
  реальной марки бренда самого приложения (красный квадрат + белая "R" -
  `src-tauri/app-icon-source.svg`, повторяет разметку `LoginPage.tsx`), а
  не из `public/favicon.svg` - тот, как выяснилось при поиске источника
  иконки, вообще не связан с брендом приложения (фиолетовая абстрактная
  фигура, похоже, unused leftover шаблона Vite/Fable, не то, что видит
  пользователь в самом приложении). iOS/Android-варианты иконок из вывода
  генератора удалены - мобильные таргеты не в планах, незачем засорять репо.
  Проверено **не только `cargo check`** (тот прошел чисто), а полным
  сквозным прогоном: `npx tauri dev` поднял настоящее нативное окно (GTK/
  WebKit, реальный скриншот окна снят через `import -window`), внутри - тот
  же React-фронт. Временный self-test-код (не часть фичи, удален после
  проверки) прогнал через реальный `invoke` оба направления: `GET
  /my/account.json` (JSON туда-обратно) и `POST /uploads.json` с бинарным
  телом (`Content-Type: application/octet-stream`, как у реальной загрузки
  вложения) - оба дошли до локального тестового Redmine и вернули
  настоящие данные (аккаунт админа, реальный upload-токен), подтверждая
  весь путь: React → `tauriFetch` → `invoke` → `proxy_request` (Rust,
  `reqwest`) → Redmine → тот же путь обратно, включая base64-кодирование
  бинарного тела в обе стороны.
- [x] **GitHub Actions** (`.github/workflows/desktop-release.yml`) - матрица
  win/macOS/linux через `tauri-apps/tauri-action`. **Только ручной запуск**
  (`workflow_dispatch`), не на каждый пуш в `main` - зафиксировано
  пользователем 2026-08-13 явно ("не надо это запускать" автоматически) -
  десктоп-дистрибутив нужен не на каждый коммит, а по решению, когда готов
  релиз; матрица на трёх платформах - не бесплатные минуты CI, чтобы жечь их
  просто так.
  - **Версия - обязательный ручной ввод** (`inputs.version`, semver без "v"),
    не берётся из git-тега/пуша - то самое "с версией", что просил
    пользователь. Прокидывается в `npm version --no-git-tag-version` перед
    сборкой на каждой платформе матрицы; `src-tauri/tauri.conf.json` больше
    **не хранит своё независимое поле `version`** (убрано намеренно) - Tauri
    подхватывает версию из `package.json`, если она не указана явно в
    конфиге, так что один `npm version` синхронизирует обе стороны (npm-
    пакет и Tauri-бандл) без риска разъехаться.
  - Тег/релиз - `redfine-v<version>`, **релиз создаётся как `draft`**
    (`releaseDraft: true`) - сборка матрицы сама по себе не публикует релиз
    пользователям, кто-то должен глазами проверить артефакты и нажать
    "Publish" в GitHub UI. Есть чекбокс `prerelease` (необязательный, по
    умолчанию `false`) - мелкое, но дешёвое добавление, не отдельная фича.
  - `concurrency` group по версии - два случайных параллельных запуска на
    одну и ту же версию не гонятся за один и тот же тег одновременно
    (последовательно, не отменяют друг друга - `cancel-in-progress` не
    включен намеренно, лучше подождать, чем оборвать сборку на полпути).
  - macOS - `--target universal-apple-darwin` (один артефакт под Apple
    Silicon и Intel сразу, стандартная практика Tauri для `macos-latest`
    раннера). Linux - `ubuntu-22.04` (не `ubuntu-latest`/24.04) - это
    рекомендация самого Tauri ради совместимости с более старым glibc у
    пользователей (собранный на более новом glibc бинарник не запустится на
    более старой системе, обратное - запускается). Системные зависимости
    под Linux (`libwebkit2gtk-4.1-dev` и т.д.) - тот же список, что
    установили руками в этой сессии для локальной сборки (см. "Жёсткий
    UI/UX-аудит" выше по документу, где искали `pkg-config`).
  - **Первый реальный запуск (2026-08-13) провалился на всех трёх
    платформах одинаково** - `Command "npm [\"run\",\"tauri\",\"build\"...]"
    failed with exit code 1`, идентично на macOS/Linux/Windows. Это сразу
    указывало не на платформенную проблему (иначе платформы падали бы по-
    разному/на разных шагах), а на что-то общее до начала реальной сборки -
    оказалось, `package.json` не содержал скрипта `"tauri"` (`tauri-action`,
    как и сам Tauri CLI, ожидает `npm run tauri build` как стандартный
    входной скрипт; локально проект гонялся через `npx tauri ...` напрямую,
    поэтому пропажа скрипта не всплыла раньше). Фикс - добавлен `"tauri":
    "tauri"` в `scripts`. **Проверено не предположением, а полным локальным
    прогоном** `npm run tauri build` (не только `dev`) на Linux - собрался
    `.deb`/`.rpm`/`.AppImage`, `.AppImage` реально запущен
    (`chmod +x` + прямой запуск, скриншот окна снят так же, как и раньше) -
    показывает тот же экран логина, что и `tauri dev`.
  - **Windows/macOS - все еще не прогнаны на реальном раннере** (нет
    доступа в этой сессии) - пользователь тестирует Windows отдельно на
    другой машине. После фикса скрипта шанс мелких платформенных неровностей
    ниже (главная известная причина одинакового краха на всех платформах
    устранена), но не нулевой - typical подозреваемые при следующей
    попытке: WebView2 на Windows (обычно уже стоит на Win10 1809+/Win11,
    отдельно не проверяли), точные версии `actions/*` под каждую ОС.
- [x] **Автообновление десктоп-приложения** (GitHub issue #2) -
  разблокировано: репозиторий сделан публичным (сверено с пользователем
  явным вопросом, чужого решения тут не принимали - см. историю
  GitHub issue #2), что сняло исходную проблему (`@tauri-apps/plugin-updater`
  качает бинарники по прямому URL без авторизации, с приватными GitHub
  Release assets так не работает).
  - **Rust** - `tauri-plugin-updater` + `tauri-plugin-process` (relaunch
    после установки) в `src-tauri/Cargo.toml`, оба зарегистрированы в
    `src-tauri/src/lib.rs` (`.plugin(...)`, по тому же паттерну, что и
    `tauri-plugin-log`). `default-features = false` + `rustls-tls` у
    `tauri-plugin-updater` - та же причина, что и у `reqwest` в `proxy.rs`
    (без системной OpenSSL). Права - `updater:default`/`process:default` в
    `src-tauri/capabilities/default.json`.
  - **Подпись обновлений** - ключевая пара сгенерирована локально
    (`npx tauri signer generate`, без пароля). Публичный ключ - в
    `src-tauri/tauri.conf.json` (`plugins.updater.pubkey`), это не секрет.
    Приватный ключ - **не в репозитории** (`src-tauri/updater.key`
    добавлен в `src-tauri/.gitignore`), уложен в GitHub Secrets репозитория
    как `TAURI_SIGNING_PRIVATE_KEY` (пустой пароль - секрет
    `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` не создавался, plugin считает его
    опциональным при пустом пароле). Если ключ или пароль когда-либо
    потеряются - подписывать новые обновления станет нечем, придётся
    генерировать новую пару и она перестанет доверяться уже установленными
    у пользователей версиями (это фундаментальное свойство схемы, не баг).
  - **`tauri.conf.json`** - `bundle.createUpdaterArtifacts: true` (сигнал
    Tauri CLI генерировать `.sig`-файлы и `latest.json` при сборке),
    `plugins.updater.endpoints` - `.../releases/latest/download/latest.json`.
    `releases/latest` у GitHub - это осознанно **опубликованный**
    (не-draft) релиз; `desktop-release.yml` по-прежнему создаёт релизы как
    draft (см. выше) - т.е. свежесобранная версия не начнёт предлагаться на
    автообновление, пока кто-то не нажмёт "Publish" в GitHub UI, тот же
    ручной контроль, что был и раньше просто задуманно распространён на
    автообновление.
  - **CI** (`desktop-release.yml`) - `tauri-apps/tauri-action` получил env
    `TAURI_SIGNING_PRIVATE_KEY`/`TAURI_SIGNING_PRIVATE_KEY_PASSWORD` из
    `secrets.*` - при их наличии и `createUpdaterArtifacts: true` в конфиге
    CLI сам подписывает бандлы и кладёт `latest.json` в тот же (draft)
    релиз, никакого отдельного шага/экшена не понадобилось.
  - **Фронт** - `src/hooks/useAppUpdater.ts` (обёртка над `check()`/
    `downloadAndInstall()`/`relaunch()` из `@tauri-apps/plugin-updater` и
    `@tauri-apps/plugin-process`; `supported` - всегда `false` в веб-сборке,
    хук ничего не делает, обновление веб-версии - это просто обновление
    страницы). Автопроверка один раз при старте приложения (без поллинга по
    таймеру - открытие приложения и так не ежеминутное событие). UI - два
    места: `UpdateBanner.tsx` (плавающий тост снизу справа, только когда
    реально есть что показать - "available"/"downloading"/"installing"/
    "error", не дергает пользователя молча) смонтирован один раз в
    `AppLayout.tsx` по образцу `HotkeysHelpDialog`; пункт "Проверить
    обновления" в дропдауне пользователя (`Topbar.tsx`, свой отдельный
    экземпляр хука - минус в виде двойного автозапроса при старте признан
    приемлемым, это один лёгкий GET). Отдельной страницы "Настройки"/"О
    программе" в проекте нет (см. "Пустые пункты сайдбара/топбара" выше) -
    решили не заводить её только ради этого, обошлись без нового раздела.
  - **Не проверено сквозным прогоном** (осознанное ограничение, не
    получится проверить локально): реальная проверка "нашлось обновление →
    скачалось → установилось → приложение перезапустилось на новой версии"
    требует **двух опубликованных** релизов подряд с разными версиями - это
    прогонится по факту на следующем настоящем релизе через
    `desktop-release.yml`, вручную это дорого имитировать. Проверено то,
    что можно проверить без реального релиза: `cargo`-код собирается по
    синтаксису (локально Rust-тулчейна на этой машине нет вообще - сборка
    Rust всегда шла только через CI, см. выше "Windows - все еще не
    прогнаны на реальном раннере"; полагаемся на CI-матрицу), `npm run
    build`/`npm run lint` проходят чисто на новом коде.

## Ведение бэклога

С 2026-08-13 открытые задачи и идеи по расширению приложения (не архитектурные
решения и не история уже сделанного - тому место в разделах выше) ведутся как
**GitHub Issues** в `htonar/redmine-redesign`, не прозой в этом файле - см.
`docs/agents/issue-tracker.md` при наличии, иначе просто `gh issue list`.
Текущий бэклог (создан 2026-08-13 по итогам предметного интервью с
пользователем - обновления, уведомления, визуал, персист состояния и т.д.):
issues #2-#15. Заводить новые идеи - тоже туда, а не сюда.

## Процесс: TDD

С 2026-08-13 (issue #15, "Тестовая инфраструктура + рефакторинг под TDD") вся
новая функциональность разрабатывается по TDD: сначала падающий тест, затем
минимальная реализация, затем рефакторинг. Не писать тесты постфактум после
готовой фичи.

## Ключевые фичи (то, чем реально пользуются каждый день)

Это не декоративные экраны (Gantt, wiki, файлы) - без них продукт не выполняет
свою основную задачу. Приоритет выше всего остального в списке шагов.

### Список задач: фильтры, сортировка, сохраненные виды

- `GET /issues.json` - основной список. Ключевые фильтры: `project_id`,
  `status_id` (есть спецзначение `o` - открытые, `c` - закрытые),
  `assigned_to_id` (спецзначение `me` - "мои задачи"), `tracker_id`,
  `priority_id`, `fixed_version_id`. Сортировка через `sort=field:desc,field2`
  (например `sort=priority:desc,updated_on:desc`). Пагинация `offset`/`limit`
  (лимит максимум 100 за раз).
- Разделение "мои задачи" vs "все задачи проекта" - это просто разные пресеты
  фильтра (`assigned_to_id=me` + текущий `project_id`), а не отдельная сущность.
  Дефолтный вид при открытии раздела задач - "мои задачи", без выбора руками.
- Сохранение фильтров - два независимых источника, оба стоит поддержать:
  1. **Свои сохраненные виды** (наш функционал, не Redmine): пресет = имя +
     сериализованный набор фильтров/сортировки/колонок. Хранить в localStorage
     на первом этапе (ключ по URL инстанса + пользователю), т.к. Redmine REST
     API не дает создавать `Queries` через API - только читать. Это то самое
     "не гадать каждый раз, где мои задачи, а где чужие" - сохраненный вид
     открывается в один клик.
  2. **Родные Query Redmine**: `GET /queries.json` возвращает уже созданные в
     самом Redmine публичные/личные query пользователя. `GET
     /issues.json?query_id=N` применяет фильтры сохраненного query одним
     параметром (остальные фильтры в этом случае игнорируются - см. спеку).
     Если у пользователя уже есть query, настроенные годами в Redmine, глупо
     заставлять его пересоздавать их у нас - просто даем выбрать query_id.
- `GET /issue_statuses.json`, `/trackers.json`,
  `/enumerations/issue_priorities.json`, `/projects.json` - справочники для
  выпадающих списков в панели фильтров.

### Учет трудозатрат (time tracking)

- Просмотр: `GET /time_entries.json`, фильтры `user_id=me` (свои), `project_id`,
  `spent_on` либо шорткаты `from`/`to` (диапазон дат - готовые пресеты "сегодня",
  "эта неделя" и т.п. строятся поверх этого), `issue_id`. Дает список записей
  (дата, проект, задача, активность, часы, комментарий) - разбить по дням/неделям
  на фронте для читаемости, API отдает плоским списком.
- Создание записи: `POST /time_entries.json` с `issue_id` (или `project_id`),
  `hours`, `activity_id`, `spent_on`, `comments`. Быстрый ввод времени прямо с
  карточки задачи (issue) - это стандартный сценарий "закрыл кусок работы,
  сразу залогировал", не отдельная форма где-то в другом разделе.
- Правка/удаление своей записи: `PUT`/`DELETE /time_entries/{id}.json`.
- Список видов деятельности для дропдауна: `GET
  /enumerations/time_entry_activities.json`.
- Сводка "сколько наработал за неделю/месяц" - считается на фронте агрегацией
  списка `time_entries` по датам, отдельного summary-эндпоинта в API нет.
